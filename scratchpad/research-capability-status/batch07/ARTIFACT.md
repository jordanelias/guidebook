# Pipeline Anatomy — published diagram

https://claude.ai/code/artifact/7e632066-cc13-45ab-8bea-fc9a4fe3ed63

Four figures, derived 2026-09-13 from `governance/pipeline-contract.yaml`,
`governance/check-registry.yaml`, `scripts/db.py`, `scripts/assess/assess_cell.py` and
`schemas/tier_derivation.py`:

1. **Process flowchart** — the seven stages, each with the key it turns on, the tables it writes,
   its writer and its blocking gate. Shows the re-entrant edge, and the hole under `synthesis`,
   which is the one stage with no blocking gate of its own.
2. **Layer map** — the five layers as horizontal bands crossed by the seven stages as vertical
   columns. Layers 0 and 1 span every stage; Layers 2 and 3 are per-stage. Stages and layers are
   perpendicular axes and the diagram exists mainly to stop them being read as one.
3. **Cell state graph** — undetermined → pending / provisional / stated, plus `not_applicable`,
   with `idx_spec_row_identity` as the terminal constraint: no re-determination path exists.
4. **Source lifecycle** — hit → candidate / admitted → verified → extracted → governing /
   down-weighted / non-anchoring, with the tier gate sitting between extracted and governing.

**Not generated by repository tooling**, so it carries the same staleness risk as any hand-written
document (CLAUDE.md rule 7). It is stamped with its derivation date and says so in its own footer.
Re-derive rather than quote. Kept as a pointer here rather than as a committed HTML file precisely
so it cannot drift silently beside `tools/`, which IS generated.
