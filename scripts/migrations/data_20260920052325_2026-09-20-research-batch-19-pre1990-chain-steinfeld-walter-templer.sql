-- data_20260920052325_2026-09-20-research-batch-19-pre1990-chain-steinfeld-walter-templer.sql
-- Data migration generated by scripts/emit_data_migration.py
-- Session:    session_2026-09-20-research-batch-19-pre1990-chain-steinfeld-walter-templer.md
-- Generated:  2026-09-20T05:23:25+00:00
-- Summary:    batch 19: GAP-013 measured -- 79 of 97 evidence_sources columns reachable by no sanctioned writer
--
-- This file is forward-only and immutable once committed. If a correction is
-- needed, emit a NEW data migration that compensates — do not edit this file.

-- Research batch delta, captured by scripts/research/emit_batch_sql.py
-- Captured:  2026-09-20T05:23:25+00:00
-- Scratch:   /tmp/claude-0/-home-user-guidebook/ca1ae452-3545-55f6-8566-12bf9a91f730/scratchpad/guidebook.db
-- Canonical: data/guidebook.db (schema version 91)
-- Totals:    0 insert(s), 1 update(s)
--
-- No transaction wrapper: the migration runner owns the boundary (F5).
-- Feed this to: python3 scripts/emit_data_migration.py --input <this file>

-- gaps: 0 insert(s), 1 update(s)
UPDATE "gaps" SET "description" = 'FOUR DEEPER FIXES A /simplify PASS NAMED AND THIS SESSION DID NOT TAKE, each wider than the diff it reviewed. (1) _AMENDABLE and _CORRECTABLE are hand-curated tuples in db.py -- the ''list maintained alongside the thing it describes'' rule 8 forbids, and rule 8 already names amend-source as an outstanding violator. This session GREW _AMENDABLE by one (jurisdiction) rather than deriving it. The named fix: declare the provenance class on the field in schemas/evidence_source.py (json_schema_extra={''provenance'':''judgement''}) and have both tuples read the model, so a new column cannot land in neither list silently -- the same move rule 8 records as the fix for dbcore.WRITABLE_TABLES, and validate_pydantic_schemas already walks model-against-table so the drift gate comes free. (2) evidence_sources.jurisdiction has NO CHECK constraint, so widening who may amend it widened write access to a column whose vocabulary is not in the column; validate_jurisdiction is an after-the-fact scan, not a write-time gate, and the comment added this session overstates it. Fix: a migration adding the CHECK mirroring schemas/enums.py JurisdictionCode, after which dbcore.check_values supplies the vocabulary to every reader. (3) source_slug_links has nowhere to DECLARE a slug''s local_ref_id label scheme, so _next_local_ref_id infers it per call and the mixed-scheme case can only refuse. Fix: slugs.local_ref_prefix (+ width) with a CHECK, which also subsumes GAP-285''s missing UNIQUE(slug, local_ref_id) -- source_slug_links_duplicates.py reports duplicated labels under one slug today. (4) The relevance_note backfill path lives inside a create verb and is the THIRD hand-written amend mechanism (amend_source, amend_extraction, this), each with its own argument names and none sharing a ledger. Fix: one amend(table, key, field, value, reason) whose amendable set is derived per table, per (1). ALSO OWED: walk.py is cited by a checked-in workplan as its rule-7a generator but lives under scratchpad/, which schema_reference_audit SKIPs and derived_not_curated_audit''s WRITER_ROOTS excludes -- it names ~30 tables and columns in raw SQL, so a rename sweeps every other reader and leaves it silently broken, which is rule 4''s exact failure mode. It was NOT moved to scripts/ here because an unregistered executable under scripts/ is itself the defect rule 8 names; the move and the check-registry entry are one job, not two. || CORRECTED 2026-09-20: MEASURED 2026-09-20, and item (1) of this gap is now the largest un-taken mechanism fix in the writer layer. THE NUMBERS, derived rather than quoted: evidence_sources has 97 columns; 10 are in db.py _AMENDABLE and 8 in _CORRECTABLE, so SEVENTY-NINE ARE REACHABLE BY NO SANCTIONED WRITER AT ALL. Re-derive with a set difference over PRAGMA table_info against the two literals in db.py. THE GROWTH CADENCE IS THE DEFECT SIGNATURE: verification_disposition (2026-09-02), jurisdiction (09-18), verification_status and doi_resolution_outcome (09-18), source_type (09-20), root_type and root_ref_id (09-20) -- seven additions in eighteen days, and EVERY ONE was provoked by a row that had already been written wrong and had cost a compensating migration. The list is not protecting anything; it is a record of which columns someone has already been burned by. THE DEEPER CHANGE, named so the next session does not have to re-derive it: invert the default. Amendable = live columns from PRAGMA table_info MINUS _CORRECTABLE keys MINUS a NAMED exclusion set, each exclusion carrying its one-line reason -- ref_id (identity), created_*/updated_* (the ledger, written by the writer), the author columns (correct-source domain and the migration-063 tombstones), superseded_by_ref_id (retirement machinery), the tool-written verification timestamps and counters, url_match_similarity (computed). That is verbatim the move rule 8 records as the fix for dbcore.WRITABLE_TABLES: "it is now derived, and the burden of proof moved to the exclusions." SAFETY DOES NOT LIVE IN THE MEMBERSHIP TUPLE and that is the argument for doing it: it lives in dbcore.check_declared, in dbcore.fk_declared (added 2026-09-20), in the I4-style per-field refusals and in the ledger, all of which are per-field and all of which stay. WHY BATCH 19 DID NOT DO IT, stated rather than skipped: inverting the default widens the write surface across 97 columns in one change, and a wrong exclusion makes a bad value writable that no gate catches. That is a change to make deliberately at the start of a session with its own adversarial pass, not at the end of a long one. ONE CORRECTION TO THIS GAP OWN PROPOSED FIX: it suggests json_schema_extra provenance metadata on schemas/evidence_source.py. That model mirrors roughly twenty of the 97 columns, so it cannot be the home today without completing the model first. The complement derivation needs no new metadata and can land on its own.', "updated_at" = '2026-09-20 05:23', "updated_by_session" = 'session_2026-09-20-research-batch-19-pre1990-chain-steinfeld-walter-templer' WHERE "gap_id" = 'GAP-013';