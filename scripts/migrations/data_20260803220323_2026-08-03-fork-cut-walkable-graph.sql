-- data_20260803220323_2026-08-03-fork-cut-walkable-graph.sql
-- Data migration generated by scripts/emit_data_migration.py
-- Session:    session_2026-08-03-fork-cut-walkable-graph
-- Generated:  2026-08-03T22:03:23+00:00
-- Summary:    backfill cell_source_links from governing_refs (63 rows, 14 cells)
--
-- This file is forward-only and immutable once committed. If a correction is
-- needed, emit a NEW data migration that compensates — do not edit this file.

BEGIN TRANSACTION;

-- Populate cell_source_links from the governing_refs JSON arrays.
--
-- Mechanical: json_each over the existing column, no judgment calls. Verified
-- before emitting — 63 entries, 14 cells, zero orphans, zero malformed, zero
-- intra-cell duplicates.
--
-- created_at is set explicitly to a fixed literal rather than datetime('now')
-- so that a rebuild reproduces these rows byte-for-byte. See 044's header.

INSERT INTO cell_source_links (cell_id, ref_id, role, created_at, created_by_session)
SELECT ecs.cell_id,
       je.value,
       'governing',
       '2026-08-03 21:36',
       'session_2026-08-03-fork-cut-walkable-graph'
FROM evidence_cell_state ecs, json_each(ecs.governing_refs) je
WHERE ecs.governing_refs IS NOT NULL AND ecs.governing_refs NOT IN ('', '[]');

COMMIT;
