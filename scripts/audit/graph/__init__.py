# scripts/audit/graph — the vectorized structural audit tool (stdlib graph spine).
# Modules are imported top-level after the package dir is placed on sys.path by
# build.py / graph_audit.py, matching the repo convention of running scripts directly.
