# retired-vocabulary.yaml — tokens that are READABLE AND WRONG.
#
# STATUS: OPERATIVE. Consumed by scripts/audit/retired_vocabulary_audit.py, which
# is registered in governance/check-registry.yaml as `retired_vocabulary` at level
# `advisory` (house norm: a newly-wired check starts advisory).
#
# WHAT THIS FILE IS FOR
# ---------------------
# The failure mode it addresses is not untidiness. It is this: a session greps for
# a fact, gets several answers, and cannot tell which is current. Every entry below
# is a string that still returns hits on the live surface and whose hits are wrong —
# a column that no longer exists, a workflow that was folded into another, a
# database path that was never the database, a population umbrella that doctrine
# bans. The cost of each is measured in the same unit: a wrong answer delivered
# confidently to whoever greps next.
#
# ADMISSION TEST — an entry belongs here only if ALL FOUR hold
# -----------------------------------------------------------
#   1. The token is retired by a NAMED authority (a DR, a migration, a dated
#      consolidation commit) — not by anyone's sense that it reads oddly.
#   2. The REPLACEMENT HAS ALREADY LANDED. A token retired by a decision whose
#      implementing migration has not run is still describing live data correctly.
#      Flagging it produces noise and teaches the reader to ignore the check.
#   3. A grep of the live surface today returns hits that are WRONG IN CONTEXT —
#      not merely historical.
#   4. The token can be matched MECHANICALLY without a judgment call. If telling a
#      true hit from a false one needs a human read of the surrounding paragraph,
#      it is not a tripwire; it is a backlog item.
#
# Entries that fail test 2 or 4 belong in `deferred:` or `rejected:` at the bottom,
# WITH THEIR REASONING. Those two sections are not filler. They exist so the next
# session does not spend an hour re-deriving a conclusion already reached, and then
# add an entry that floods the check. Read them before adding anything.
#
# MENTION vs USE
# --------------
# A document that RETIRES a token must be able to name it. Three mechanisms:
#   * global `exempt_paths` — immutable and historical records, listed below
#   * per-entry `exempt_paths` — the specific live files licensed to say it
#   * the inline escape `[RETIRED-VOCAB-OK]` anywhere on the same line
# The inline escape is deliberately noisy to type. Reaching for it often is a
# signal the entry is wrong, not that the file is special.
#
# THE TWO LICENSED MENTIONS — read this before adding an exemption
# ----------------------------------------------------------------
#   A. THE DOCUMENTS THAT RECORD THE RETIREMENT. The paper trail cannot
#      describe a retirement without naming what was retired.
#   B. FILES WHERE THE TOKEN IS STILL CORRECT. A crosswalk that maps the old
#      value to the new one must hold the old value; a scratch-path default is
#      the right use of a scratch path; a test fixture may legitimately seed a
#      retired value to prove the checker catches it.
#   Nothing else. In particular, NOT "this file has too many hits to fix".
#
#   Category B was added 2026-08-05 after an adversarial review pointed out that
#   RV-009 already exempted `scripts/tests/**` and `bootstrap.sh` on exactly this
#   ground, while the rule text admitted only category A — so the stated rule and
#   the actual exemption set disagreed. Declaring the category is the honest fix;
#   quietly widening category A would have been the dishonest one.
#
# So `ci.yml`'s header may say what it absorbed; `check-registry.yaml`'s header
# may say which four lists it replaced; CLAUDE.md may narrate the 2026-08-01
# fold. Those files are the retirement's paper trail and cannot describe it
# without naming it. A workplan that USES a retired name as a live instruction
# is not covered, no matter how convenient the exemption would be. The
# distinction is exactly mention-vs-use, and getting it wrong in the permissive
# direction turns this file into a way of making the check green rather than a
# way of making the repo true.
#
# Corollary, learned while first tuning this register: per-file exemptions are
# too COARSE for source files, which routinely both use and discuss a token —
# scripts/db.py names `doi_less_key` in a comment explaining that it strips the
# key (licensed) AND names `applicable_groups` in a live write path (a defect).
# Exempt per entry, never blanket, and use the inline escape for single lines.
#
# COUNTS IN `note:` FIELDS are DATED MEASUREMENTS, not invariants — same convention
# as check-registry.yaml. Use one to decide where to look, never to report state.

schema_version: 1

# --- global exemptions ------------------------------------------------------
# Paths whose whole purpose is to preserve text as written. Sweeping these would
# contradict the repo's forward-only, immutable-record convention — the same
# principle DR-2026-07-21 §4 invoked when it refused to rewrite `E-##` in past
# records, and the reason migrations are append-only (CLAUDE.md §4).
exempt_paths:
  - "_archived/**"                 # retired-but-preserved, mirrors origin paths
  - "scripts/migrations/**"        # append-only, immutable once committed
  - "sessions/**"                  # append-only session records
  - "decisions/**"                 # DRs are the record OF a retirement
  - "data/decisions/decision_register.yaml"   # the shadow store of decisions/, same class
  - "attestations/**"              # append-only adherence log; reattestation[] is forward-only
  - "references/project-standards.md"         # THE append-only operative rule ledger (CLAUDE.md §5)
  - "audits/**"                    # dated reports, frozen at their date
  - "references/audits/**"         # ditto
  - "references/search-log/**"     # verbatim query logs; R8 forbids backfilling
  - "workplan/_superseded/**"      # superseded by definition
  # A DATED workplan records a plan as it stood on that date, exactly as a dated
  # audit records findings as they stood. Rewriting `audit.yml` inside a
  # 2026-05-17 build plan would falsify what was actually planned in May. UNDATED
  # workplans are NOT exempt: they read as standing instructions, and
  # workplan/workplan-item-audit-pipeline-co0009.md is the worked example — it
  # names both a retired column and a database path that does not exist.
  - "workplan/*20??-??-??*.md"
  # Same rule, same reasoning, for a plan split across a DATED DIRECTORY rather
  # than one dated file — workplan/execution-plan-2026-08-12/** is the live
  # example, and its member files carry undated names (4-adjudication-apparatus.md)
  # so the glob above never reached them. Added 2026-08-14 when three new entries
  # flagged that directory; it silences one pre-existing RV-008 hit there too,
  # which on reading is a licensed mention (it names the bad path to report it).
  # A superseded proposal inside such a plan gets a dated forward note in the
  # document — the reader is protected by the note, not by the grep.
  - "workplan/*20??-??-??*/**"
  - "versions/**"                  # historical version snapshots
  # The two documented legacy script directories. CLAUDE.md §7 places both
  # outside the GUIDEBOOK_DB_PATH contract, and scripts/migrate/** is disarmed by
  # scripts/migrate/_legacy_guard.py. They encode the PRE-SQLite schema on
  # purpose: rewriting them would falsify the import they record, and commit
  # 366766ee deliberately kept them because architecture/sqlite-data-layer.md §9
  # names six of them in its build table. Guarded, not corrected.
  - "scripts/migrate/**"
  - "scripts/db/**"
  - "**/__pycache__/**"
  - ".git/**"
  - "governance/retired-vocabulary.yaml"           # this file names every token
  - "scripts/audit/retired_vocabulary_audit.py"    # so does its reader

# --- active entries ---------------------------------------------------------
entries:

  - id: RV-001
    token: applicable_groups
    match: identifier
    severity: broken
    retired_by: "schema migration — column dropped from `items`"
    replacement: "item_population_links (the junction that replaced the packed column)"
    note: >-
      Not a naming preference: the column DOES NOT EXIST in the live schema
      (verified against data/guidebook.db 2026-08-05 — all 67 tables and all 18
      views; a view could otherwise still expose it). Code that
      names it does not return a stale answer — it raises OperationalError.
      This is exactly how scripts/migrate/migrate_items.py was discovered: it took
      no required arguments, opened the canonical DB immediately, and was saved
      from writing it only by crashing on this column name. Measured 2026-08-05:
      17 files repo-wide before exemptions.

  - id: RV-002
    token: doi_less_key
    match: identifier
    severity: broken
    retired_by: "schema migration — column dropped from `evidence_sources`"
    replacement: "no direct successor; dedup is by DOI, then by normalised title"
    exempt_paths:
      - "CLAUDE.md"                              # §7 names the crash it causes
      - "governance/check-registry.yaml"         # quarantine note quotes the error
      - "references/tooling-register.md"         # ditto
      - "scripts/tests/test_db_integrity.py"     # comment explains why C5 lapsed
      - "scripts/db.py"                          # comment on the line that STRIPS the key
      - "scripts/migrate_evidence_sources_v2.py" # one-time migrator FROM the old schema
      # The 2026-05-05 pre-implementation DESIGN document. Its CREATE TABLE blocks
      # are what was specified, not what was built; a DDL banner at the head of the
      # file says so and points at the live schema. Preserved rather than corrected,
      # for the same reason as the proposal-of-record documents under RV-008.
      - "architecture/sqlite-data-layer.md"
    note: >-
      Same class as RV-001 — the column is absent from every live table. This is
      the name that breaks scripts/validate_db.py (`no such column: doi_less_key`),
      which is why that script is quarantined in check-registry.yaml rather than
      run. A crashing gate reads as noise; see the registry's `broken` status note.
      validate_db.py is deliberately NOT exempted: it does not discuss the column,
      it queries it, and the check should keep saying so until the script is fixed
      or retired (an owner decision — see the registry's `broken` status).
      scripts/migrate_evidence_sources_v2.py is exempt on different grounds: it is
      a one-time migrator that reads the OLD schema by design, and its inputs are
      /home/claude/* paths that do not exist in this tree. It belongs in
      _archived/ alongside its siblings, but archiving is owner-gated.

  - id: RV-003
    token: audit.yml
    match: literal
    severity: stale-pointer
    retired_by: "2026-08-01 workflow consolidation (8 workflows -> 4)"
    replacement: "ci.yml — the `classify` job plus the battery jobs it selects"
    # The consolidation's paper trail — these four cannot describe the fold
    # without naming what was folded. Nothing else may be added here.
    exempt_paths:
      - "_archived/workflows/README.md"   # the mapping doc; naming it is its job
      - ".github/workflows/ci.yml"        # header states what it absorbed
      - "governance/check-registry.yaml"  # header states which four lists it replaced
      - "references/tooling-register.md"  # the four-list comparison table
      - "CLAUDE.md"                       # §0 and §7 narrate the fold
      # Historical PI snapshots. CLAUDE.md §10: the numbered files are snapshots and
      # the HIGHEST-numbered one is the deployed copy — so v10_14 is deliberately NOT
      # exempt. Its stale workflow names are what a claude.ai session actually loads,
      # which makes them the highest-value occurrence in this whole register. When a
      # v10_15 ships, add v10_14 to this list; do not exempt the live PI.
      - "governance/project-instructions-v10_7.md"
      - "governance/project-instructions-v10_8.md"
      - "governance/project-instructions-v10_9.md"
      - "governance/project-instructions-v10_10.md"
      - "governance/project-instructions-v10_11.md"
      - "governance/project-instructions-v10_12.md"
      - "governance/project-instructions-v10_13.md"
    note: >-
      The highest-volume wrong answer in the repo. A session asking "which workflow
      runs the audits" gets dozens of hits naming a file that has not existed since
      2026-08-01. CLAUDE.md §0 carried this error itself until 2026-08-02, where it
      contradicted §7 two hundred lines below. Measured 2026-08-05: 45 files
      repo-wide before exemptions, concentrated in governance/, workplan/ and
      references/ — i.e. on the live surface, not only in frozen records.

      KNOWN OWNER-GATED RESIDUE, and the reason this entry is expected to stay red:
      governance/project-instructions-v10_14.md names the retired workflow five
      times. It is the DEPLOYED PI (highest-numbered wins, CLAUDE.md §10) and so
      the highest-value occurrence in this register — a claude.ai session loads it
      and is pointed at a workflow that has not existed since 2026-08-01. It is
      deliberately NOT exempted and deliberately NOT edited. The numbered PI files
      are snapshots, so correcting one in place breaks the convention that a new
      version means a new file; and the PI is not API-writable — the owner pastes
      it into claude.ai, so a repo-side edit does not reach the surface that
      matters. The real fix is a v10_15 the owner pastes. Until then this check
      reports the defect rather than hiding it, which is the correct behaviour for
      a gate that must not act unilaterally. When v10_15 ships, add v10_14 above.

  - id: RV-004
    token: research-contract.yml
    match: literal
    severity: stale-pointer
    retired_by: "2026-08-01 workflow consolidation"
    replacement: "ci.yml — the `research` battery"
    exempt_paths:                          # same paper trail as RV-003
      - "_archived/workflows/README.md"
      - ".github/workflows/ci.yml"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "CLAUDE.md"
      # Historical PI snapshots. CLAUDE.md §10: the numbered files are snapshots and
      # the HIGHEST-numbered one is the deployed copy — so v10_14 is deliberately NOT
      # exempt. Its stale workflow names are what a claude.ai session actually loads,
      # which makes them the highest-value occurrence in this whole register. When a
      # v10_15 ships, add v10_14 to this list; do not exempt the live PI.
      - "governance/project-instructions-v10_7.md"
      - "governance/project-instructions-v10_8.md"
      - "governance/project-instructions-v10_9.md"
      - "governance/project-instructions-v10_10.md"
      - "governance/project-instructions-v10_11.md"
      - "governance/project-instructions-v10_12.md"
      - "governance/project-instructions-v10_13.md"
    note: "Folded with RV-003. Measured 2026-08-05: 11 files repo-wide."

  - id: RV-005
    token: regenerate-vetting-surface.yml
    match: literal
    severity: stale-pointer
    retired_by: "2026-08-01 workflow consolidation"
    replacement: "regenerate-derived.yml — leg 1 of the 3-leg matrix"
    exempt_paths:
      - "_archived/workflows/README.md"
      - ".github/workflows/regenerate-derived.yml"   # header states what it absorbed

  - id: RV-006
    token: regenerate-evidentiary-audit.yml
    match: literal
    severity: stale-pointer
    retired_by: "2026-08-01 workflow consolidation"
    replacement: "regenerate-derived.yml — leg 2 of the 3-leg matrix"
    exempt_paths:
      - "_archived/workflows/README.md"
      - ".github/workflows/regenerate-derived.yml"   # header states what it absorbed

  - id: RV-007
    token: regenerate-pipeline-completeness.yml
    match: literal
    severity: stale-pointer
    retired_by: "2026-08-01 workflow consolidation"
    replacement: "regenerate-derived.yml — leg 3 of the 3-leg matrix"
    exempt_paths:
      - "_archived/workflows/README.md"
      - ".github/workflows/regenerate-derived.yml"   # header states what it absorbed

  - id: RV-008
    token: data/db/guidebook.db
    match: literal
    severity: stale-pointer
    retired_by: "superseded by data/guidebook.db as the canonical store"
    replacement: "data/guidebook.db"
    exempt_paths:
      - "CLAUDE.md"                              # §7 documents the hazard by name
      - "scripts/audit/db_path_env_audit.py"     # scopes the contract by this path
      - "scripts/migrate/_legacy_guard.py"       # quotes §7 to justify the guard
      - "skills/item-specification-writer_SKILL.md"   # warns sessions off it by name
      # Proposal-of-record documents from 2026-05-02 whose execution landed
      # elsewhere. Each now carries an OUTCOME BANNER at its head saying so; the
      # proposal body is preserved verbatim, because rewriting a proposal to match
      # its own outcome destroys the record of what was proposed.
      - "governance/repo-strategy-revision-co.md"
      - "architecture/schema-spec.md"
    note: >-
      A path that has never existed on disk in this working tree, presented in
      several documents as though it were a database. CLAUDE.md §7 correctly warns
      that scripts/db/** targets it and calls it "a different, legacy file" — that
      warning is the only licensed mention.

  - id: RV-009
    token: /tmp/guidebook.db
    match: literal
    severity: stale-pointer
    retired_by: "operational — a scratch path is not the canonical DB"
    replacement: "file:data/guidebook.db?mode=ro for reads; a migration for writes"
    exempt_paths:
      - "CLAUDE.md"
      - "scripts/audit/db_path_env_audit.py"
      - "scripts/migrate/_legacy_guard.py"
      # Correct uses of a scratch path, which is what /tmp is for:
      - "scripts/bootstrap.sh"            # downloads a COPY of the DB and reads it
      - "scripts/verify_resolved_dois.py" # defensive default; resolve-dois.yml sets the real path
      - "scripts/tests/**"                # fixture DBs
    note: >-
      This is stale context that ACTIVELY MISLEADS rather than merely ageing: a
      skill instructing a session to open /tmp/guidebook.db sends it to a file that
      does not exist, and the failure looks like a broken environment rather than a
      wrong instruction. Purged from skills/workplan-orchestrator_SKILL.md in
      60bf6b75; this entry stops it coming back. Note that the tokens in
      scripts/tests/** are fixture paths for scratch DBs, which is the correct use —
      hence `severity: stale-pointer`, and read the hit before editing it.

  # --- D-0157 verification vocabulary (RV-012..RV-016) ----------------------
  # PROMOTED FROM `deferred:` ON 2026-08-05, AND THE DEFERRAL WAS WRONG.
  #
  # The deferral asserted that the DR §5 migration "has NOT RUN" and that "863
  # evidence_sources rows still carry this vocabulary". Both false. The migration
  # ran on 2026-08-04 — `scripts/migrations/data_20260804164915_2026-08-04-d0157-
  # corrections.sql`, preceded by two remap migrations the same afternoon — and the
  # live column holds exactly two values: VERIFIED (752) and UNVERIFIED (111).
  #
  # HOW THE ERROR HAPPENED, because the mechanism matters more than the fact: the
  # deferral was written by copying the DR's own status block, which still reads
  # "Ratified != executed ... B01/B02/B05 stay red until it does". That line was
  # true when the DR was drafted and is frozen there correctly — decisions/ is an
  # append-only record. The register is NOT. Reading a frozen document for a
  # volatile fact, instead of querying the database, is the precise failure this
  # whole file exists to catch, committed by this file. CLAUDE.md's standing
  # instruction covers it in one line: when a fact is volatile, derive it from the
  # live repo rather than trusting a number written down.
  #
  # The cost was not hypothetical. The deferral also said "Do not promote one
  # without checking its precondition", so an obedient session would have TRUSTED
  # it and left the largest wrong-answer class on the surface unswept.
  #
  # Substring trap, pre-solved: `match: identifier` is required, not optional — a
  # literal search for VERIFIED-1 also matches UNVERIFIED-1, and for
  # VERIFIED-WITH-CORRECTION it would double-count. See build_pattern().

  - id: RV-012
    token: UNVERIFIED-1
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157), executed by migration data_20260804164915"
    replacement: >-
      UNVERIFIED, plus verification_disposition='OPEN'. The suffix claimed to mean
      "one attempt, not yet retried"; verification_attempt_count already records
      attempts, and contradicted the suffix on 26 of 31 rows.
    note: >-
      The highest-volume member of this family and the one that does live damage:
      skills/gap-driven-mining_SKILL.md gates closure on
      `verification_status IN ('VERIFIED','UNVERIFIED-1')`, which since 2026-08-04
      matches nothing. A gate that silently matches nothing is the same defect
      class as a gate with nothing in scope.
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "governance/migration-survival.md"
      - "architecture/schema-spec.md"
      - "schemas/enums.py"
      - "schemas/evidence_state.py"
      - "scripts/tests/test_db_integrity.py"
      - "scripts/validate_verification_consistency.py"
      - "scripts/verify_resolved_dois.py"
      - "scripts/audit_evidence_metadata.py"
      - "scripts/generate/spec_page.py"
      - "scripts/generate/population_page.py"
      - "tools/regenerate_vetting_surface.py"
      - "governance/evidence-methodology.md"      # carries the D-0157 amendment banner
      - "governance/co1-operational.md"           # ditto
      - "skills/integrity-protocol_SKILL.md"      # narrates the incident as a lesson
      - "skills/evidence-metadata-rehabilitation_SKILL.md"
      - "skills/gap-driven-mining_SKILL.md"
      - "skills/reasoning-doc-citations_SKILL.md"
      # B — the token is still correct here
      - "scripts/convert/convert_sources.py"      # the old->new crosswalk itself

  - id: RV-013
    token: VERIFIED-1
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157), executed by migration data_20260804164915"
    replacement: "VERIFIED, plus verification_method recording HOW it was established"
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "governance/migration-survival.md"
      - "architecture/schema-spec.md"
      - "schemas/enums.py"
      - "schemas/evidence_state.py"
      - "scripts/tests/test_db_integrity.py"
      - "scripts/validate_verification_consistency.py"
      - "scripts/verify_resolved_dois.py"
      - "scripts/audit_evidence_metadata.py"
      - "scripts/generate/spec_page.py"
      - "scripts/generate/population_page.py"
      - "tools/regenerate_vetting_surface.py"
      - "governance/evidence-methodology.md"      # carries the D-0157 amendment banner
      - "governance/co1-operational.md"           # ditto
      - "skills/integrity-protocol_SKILL.md"      # narrates the incident as a lesson
      - "skills/evidence-metadata-rehabilitation_SKILL.md"
      - "skills/gap-driven-mining_SKILL.md"
      - "skills/reasoning-doc-citations_SKILL.md"
      # B — the token is still correct here
      - "scripts/convert/convert_sources.py"      # the old->new crosswalk itself

  - id: RV-014
    token: VERIFIED-2
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157), executed by migration data_20260804164915"
    replacement: "VERIFIED, plus verification_method"
    note: >-
      The -1/-2 suffix encoded METHOD in a column that holds STANDING — the
      one-column-three-domains conflation migration 041 was written to end.
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "governance/migration-survival.md"
      - "architecture/schema-spec.md"
      - "schemas/enums.py"
      - "schemas/evidence_state.py"
      - "scripts/tests/test_db_integrity.py"
      - "scripts/validate_verification_consistency.py"
      - "scripts/verify_resolved_dois.py"
      - "scripts/audit_evidence_metadata.py"
      - "scripts/generate/spec_page.py"
      - "scripts/generate/population_page.py"
      - "tools/regenerate_vetting_surface.py"
      - "governance/evidence-methodology.md"      # carries the D-0157 amendment banner
      - "governance/co1-operational.md"           # ditto
      - "skills/integrity-protocol_SKILL.md"      # narrates the incident as a lesson
      - "skills/evidence-metadata-rehabilitation_SKILL.md"
      - "skills/gap-driven-mining_SKILL.md"
      - "skills/reasoning-doc-citations_SKILL.md"
      # B — the token is still correct here
      - "scripts/convert/convert_sources.py"      # the old->new crosswalk itself

  - id: RV-015
    token: UNVERIFIED-2
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157), executed by migration data_20260804164915"
    replacement: "UNVERIFIED, plus verification_disposition and verification_closure_reason"
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "governance/migration-survival.md"
      - "architecture/schema-spec.md"
      - "schemas/enums.py"
      - "schemas/evidence_state.py"
      - "scripts/tests/test_db_integrity.py"
      - "scripts/validate_verification_consistency.py"
      - "scripts/verify_resolved_dois.py"
      - "scripts/audit_evidence_metadata.py"
      - "scripts/generate/spec_page.py"
      - "scripts/generate/population_page.py"
      - "tools/regenerate_vetting_surface.py"
      - "governance/evidence-methodology.md"      # carries the D-0157 amendment banner
      - "governance/co1-operational.md"           # ditto
      - "skills/integrity-protocol_SKILL.md"      # narrates the incident as a lesson
      - "skills/evidence-metadata-rehabilitation_SKILL.md"
      - "skills/gap-driven-mining_SKILL.md"
      - "skills/reasoning-doc-citations_SKILL.md"
      # B — the token is still correct here
      - "scripts/convert/convert_sources.py"      # the old->new crosswalk itself

  - id: RV-016
    token: VERIFIED-WITH-CORRECTION
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157), executed by migration data_20260804164915"
    replacement: "VERIFIED, with the correction recorded in verification_note"
    note: >-
      The owner's ruling was that this value is "meaningless. it's merely
      verified" — the correction is history, and history belongs in the note
      column, not in the standing.
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "governance/check-registry.yaml"
      - "references/tooling-register.md"
      - "governance/migration-survival.md"
      - "architecture/schema-spec.md"
      - "schemas/enums.py"
      - "schemas/evidence_state.py"
      - "scripts/tests/test_db_integrity.py"
      - "scripts/validate_verification_consistency.py"
      - "scripts/verify_resolved_dois.py"
      - "scripts/audit_evidence_metadata.py"
      - "scripts/generate/spec_page.py"
      - "scripts/generate/population_page.py"
      - "tools/regenerate_vetting_surface.py"
      - "governance/evidence-methodology.md"      # carries the D-0157 amendment banner
      - "governance/co1-operational.md"           # ditto
      - "skills/integrity-protocol_SKILL.md"      # narrates the incident as a lesson
      - "skills/evidence-metadata-rehabilitation_SKILL.md"
      - "skills/gap-driven-mining_SKILL.md"
      - "skills/reasoning-doc-citations_SKILL.md"
      # B — the token is still correct here
      - "scripts/convert/convert_sources.py"      # the old->new crosswalk itself

  - id: RV-018
    token: IS-PAYWALL
    match: identifier
    severity: broken
    retired_by: "DR-2026-08-04 (D-0157) §, executed by migration data_20260804164915"
    replacement: >-
      verification_status='UNVERIFIED' with verification_disposition='CLOSED'
      and verification_closure_reason='paywalled'
    note: >-
      A paywall is why a pursuit STOPPED, not a standing a source holds. D-0157
      collapsed it into the binary and gave the reason its own column.
      Found live 2026-08-06 in `skills/evidence-metadata-rehabilitation_SKILL.md`,
      which is the worst place for it: a live INSTRUCTION to write a value the
      schema no longer recognises, in the skill whose whole job is metadata
      rehabilitation, four lines above that same file's correct narration of the
      D-0157 change. It half-updated itself and nothing noticed. Sibling tokens
      RV-012..RV-016 were swept the same day and this one was missed because the
      sweep worked from the DR's list of values WITH ROWS — `IS-PAYWALL` had zero
      rows, so it survived the data sweep and stayed in the prose that tells the
      next session to create some.
    exempt_paths:
      # A — the paper trail for this retirement
      - "decisions/DR-2026-08-04-verification-status-is-a-standing-not-a-history.md"
      - "skills/evidence-metadata-rehabilitation_SKILL.md"   # names it to correct it
      # B — the token is still correct here
      - "references/tooling-register.md"   # cites DR-2026-05-19 admitting it, as
                                           # the worked example of the ratification
                                           # pattern; the history is accurate

  - id: RV-019
    token: es.authors
    match: identifier
    severity: broken
    retired_by: "the author normalisation that created evidence_source_authors"
    replacement: "es.author_display, or es.first_author_last for sorting"
    note: >-
      Same species as RV-001/RV-002: a column that does not exist, named in a
      query that therefore raises `no such column`. Authors are normalised into
      `evidence_source_authors` (1,478 rows); the denormalised conveniences on
      `evidence_sources` are `author_display` and `first_author_last`.
      Registered as the `es.`-QUALIFIED form on purpose. The bare word `authors`
      is ordinary English and appears legitimately across the corpus, so it fails
      admission test 4; `es.authors` can only be a query against a dropped column.
      Found 2026-08-06 in `bibliography-compiler` and `citation-miner`, both of
      which are skills whose queries had never been executed — the sampling result
      that motivated the W2 skills sweep, still true.

  - id: RV-017
    token: db_meta.schema_version
    match: identifier
    severity: stale-pointer
    retired_by: >-
      W4.7 of workplan/2026-08-02-architecture-decision-and-execution-plan.md,
      executed by migration data_20260806070009_2026-08-06-w4-continuity.sql
    replacement: "PRAGMA user_version"
    note: >-
      A second schema-version marker that stopped tracking migrations after 005.
      It read 11 against a `user_version` of 52 — forty-one versions of drift,
      naming a schema that has not existed since May. Nothing read it, so it cost
      nothing at runtime and everything in legibility: CLAUDE.md §4 had to carry
      a standing "ignore this row" instruction, which is what a defect looks like
      when it has been documented instead of fixed. The row is gone from the
      committed DB and `init_db.py` no longer seeds it into new ones.
      Registered because deleting a value does not delete the SENTENCES about it,
      and this one appears in enough plans and audits that a session could still
      grep it up and treat it as an authority. Two markers is one too many
      whatever they say; two markers that DISAGREE is the wrong-answer machine
      this register exists to dismantle.
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"
      - "scripts/init_db.py"

  - id: RV-010
    token: physically disabled
    match: phrase
    severity: doctrine
    exempt_paths:
      - "governance/functional-taxonomy.md"   # §3.3 defines the ban; naming it is the point
      - "CLAUDE.md"                          # §10 quotes both umbrellas as the worked examples
    retired_by: "DR-2026-07-22-work-from-axes; RULE 2026-07-22 in references/project-standards.md"
    replacement: >-
      the specific functional axis or the specific self-identified profile —
      see governance/functional-taxonomy.md §3.3
    note: >-
      A banned umbrella, not a style preference. Doctrine's objection is that broad
      umbrellas COLLAPSE OPPOSED DEMANDS and erase specific communities — an
      ambulatory wheelchair user and a full-time wheelchair user have conflicting
      requirements that this phrase renders as one. Recorded in CLAUDE.md §10 as a
      repeatedly-caught failure mode, which is the strongest argument for a
      mechanical tripwire rather than another line of prose asking people to
      remember. Measured 2026-08-05: 7 files repo-wide before exemptions.

  - id: RV-011
    token: energy-limiting chronic illness
    match: phrase
    severity: doctrine
    exempt_paths:
      - "governance/functional-taxonomy.md"
      - "CLAUDE.md"
    retired_by: "DR-2026-07-22-work-from-axes"
    replacement: "the specific functional axis (fatigue/stamina demand) or the named profile"
    note: >-
      Named in CLAUDE.md §10 alongside RV-010 as the second worked example of the
      umbrella failure. Measured 2026-08-05: 5 files repo-wide.

  - id: RV-020
    token: evidence_cell_state
    match: identifier
    severity: broken
    retired_by: >-
      Owner ruling 2026-08-12 ((item x population) is a SPECIFICATION, not a
      "cell"), executed by schema version 055 whose DDL ships in
      scripts/migrations/data_20260812075349_2026-08-12-rename-cell-to-specification.sql
    replacement: "specifications"
    note: >-
      Same species as RV-001/RV-002/RV-019: a name that no longer exists, so a
      query naming it raises `no such table`. Registered rather than swept
      because the sweep cannot be finished by hand — the 19 immutable data
      migrations, the frozen audits and the dated workplans all name it
      correctly and forever, and only a standing check can tell those apart from
      a live surface that has drifted back. Admission test 4 is satisfiable
      precisely because it is an identifier that can only ever have been a table.
    exempt_paths:
      # A — the paper trail for this retirement
      - "CLAUDE.md"                                        # §4 names the old name once, as a pointer
      - "skills/specification-curator_SKILL.md"            # ditto, in its repointing note
      - "governance/check-registry.yaml"                   # a check note quotes the pre-rename tamper trace
      - "workplan/2026-08-12-step-R-rename-execution-record.md"
      # B — fixtures that must name it because they replay the rename onto an
      # immutable pre-055 migration's DDL. Renaming these WOULD BREAK THEM: the
      # selector must match the migration's own text. This is not a licensed
      # mention, it is a load-bearing use of the historical name.
      - "scripts/tests/test_evidence_cell_state_2_3.py"
      - "scripts/tests/test_validate_evidence_state_2_4.py"
      # The helper that REPLACED their migration-scanning fixtures. Its docstring
      # names the old table to explain the two failures that motivated it; that is
      # a licensed mention of the retirement, category A.
      - "scripts/tests/_baseline_ddl.py"
      # C — the aspirational design spec, which describes a `cell` table that
      # never shipped. Its reconciliation is architecture/schema-reconciliation.md,
      # not this register.
      - "architecture/schema-spec.md"
      # D — working records of a COMPLETED pre-rename exercise: the July
      # evidence-migration derivations and verifier verdicts, the pilot manifest,
      # and a copy of an already-committed migration. Same class as audits/ —
      # they record what was done on their date and are not instructions. They
      # are not hidden by .ignore, which is why they are named here rather than
      # left to be re-derived by whoever greps next.
      - "working/evidence-migration/**"
      - "working/pilot/**"
      - "working/claims-docket.md"

  - id: RV-021
    token: cell_source_links
    match: identifier
    severity: broken
    retired_by: "as RV-020"
    replacement: "specification_source_links"
    note: >-
      The (specification x source) junction. Registered with RV-020 because a
      half-renamed pair is worse than either name alone: a session that finds the
      new table and the old junction concludes the junction was dropped.
    exempt_paths:
      - "scripts/tests/test_validate_evidence_state_2_4.py"
      - "workplan/2026-08-12-step-R-rename-execution-record.md"

  - id: RV-022
    token: RESOLUTION-PROPOSED
    match: literal
    severity: broken
    retired_by: "owner ruling 2026-08-14; schema migration 058 — CHECK no longer admits it"
    replacement: "PROPOSED"
    exempt_paths:
      - "scripts/validate_conflict.py"   # the enforcer; its map names what it rejects
      - "scripts/db.py"                  # comment on the line carrying the new set
    note: >-
      A direct synonym retirement: nothing about the conflict's meaning moved, only
      the spelling. `severity: broken` rather than `stale-pointer` because the value
      is now refused by a CHECK constraint — a write carrying it raises IntegrityError
      rather than storing a dated word.

  - id: RV-023
    token: MODE-S-ONLY
    match: literal
    severity: broken
    retired_by: "owner ruling 2026-08-14; schema migration 058"
    replacement: "UNRESOLVED, with the Person-Mode handoff named in mode_s_trigger"
    exempt_paths:
      - "scripts/validate_conflict.py"   # the enforcer
      - "scripts/db.py"                  # comment on the new set
      - "schemas/conflict.py"            # docstring explaining the rehomed rule
    note: >-
      Retired twice over, which is why it is worth a tripwire — and the first
      retirement is the instructive one. Item V of RATIFICATION-PACKAGE-2026-07-12
      normalized "Mode P/S" to Population/Person Mode and was ratified IN FULL by
      owner directive on 2026-07-13 (RATIFICATION-RECORD-2026-07-13, A5). Item V
      named its own implementation: "96 files; one real data migration:
      conflicts.status CHECK". That migration did not run. The status word then
      survived a month in the database CHECK, both validators, the skill and five
      conflict matrices, until the 2026-08-14 ruling retired it as a status and
      migration 058 finally changed the constraint Item V had named.
      (This note said "normalized on 2026-07-21, §7 of evidence-architecture"
      until 2026-08-15. That cited a DR-gated PROPOSAL section and the wrong date;
      corrected on re-derivation.)
      The distinction it carried is NOT lost and a reader should not treat this as
      a pure rename: MODE-S-ONLY meant "irreconcilable at population scale,
      requires individual co-design", which UNRESOLVED alone does not say. That
      half now lives in `mode_s_trigger` / `unresolvable_residual`, and the swept
      matrices say it in words beside the status.

  - id: RV-024
    token: UNRESOLVABLE-MODE-S
    match: literal
    severity: broken
    retired_by: "as RV-023"
    replacement: "UNRESOLVED"
    exempt_paths:
      - "scripts/validate_conflict.py"   # the enforcer
      - "schemas/conflict.py"            # docstring explaining the rehomed rule
    note: >-
      The file layer's spelling of RV-023 — registered separately because the two
      layers spelled the same state differently, which is the divergence the
      ruling closed. `schemas/conflict.py` keyed its mode_s_trigger requirement on
      this string; that validator now keys on UNRESOLVED.

  - id: RV-025
    token: Mode P
    match: phrase
    severity: doctrine
    retired_by: >-
      Item V of RATIFICATION-PACKAGE-2026-07-12, ratified IN FULL by owner
      directive 2026-07-13 (RATIFICATION-RECORD-2026-07-13, A5)
    replacement: "Population Mode (and 'Population-Mode range' / 'Population-Mode default')"
    exempt_paths:
      - "workplan/ratification-execution-register-2026-07-13.md"  # the row that tracks the debt
    note: >-
      Registered 2026-08-15, and the gap it closes is the point. RV-023 already
      told the Item V story — a ratified decision that named its own single
      implementation step and did not take it for a month — but registered only
      the `conflicts.status` TOKEN. The design-mode SPELLING that Item V actually
      retired had no tripwire at all, which is precisely why the sweep could sit
      at "canonical core done, ~90 files remaining" from 2026-07-13 to 2026-08-15
      with every gate green. Prose does not check itself, and a workplan row is
      not a check.

      Not cosmetic. `governance/co1-operational.md` (swept 2026-08-15) defined the
      design modes AS "Tier 1" and "Tier 2" two paragraphs from sentences using
      "Tier 1" in its evidence-ladder sense — the collision Item V exists to end,
      in a governance document.

      Expect a non-zero count on first run: the `references/` corpus (BPC,
      conflict-matrices, fdr, connection registers, throughline docs) is unswept.
      That count IS the remaining Q1 debt, and it is meant to be visible rather
      than remembered. `severity: doctrine` not `broken`: nothing raises on read,
      the text is simply wrong about what the project calls its own design scales.

  - id: RV-026
    token: Mode S
    match: phrase
    severity: doctrine
    retired_by: "as RV-025"
    replacement: "Person Mode (and 'Person-Mode handoff')"
    exempt_paths:
      - "workplan/ratification-execution-register-2026-07-13.md"
    note: >-
      The Person-Mode half of RV-025, registered separately so the two can be
      exempted independently — "Mode S" survives in more licensed places than
      "Mode P" does, because the DB column `mode_s_trigger` still carries the
      retired word in its NAME. That column rename is a structural change with its
      own caller sweep and Change-Order, explicitly deferred on 2026-08-15; until
      it happens, a document naming the column is describing live data correctly
      and must not be flagged. Exempt the FILE, never the token — when the column
      is renamed, delete these exemptions rather than widening them.

# --- deferred: correct to retire, NOT YET SAFE TO FLAG ----------------------
# These fail admission test 2 — the replacement has not landed — so flagging them
# today would flag accurate descriptions of live data. Each records the precondition
# that would promote it. Do not promote one without checking its precondition.
deferred:

  - tokens: [WITHDRAWN, PROVISIONAL, OPEN]
    retired_by: >-
      Owner ruling 2026-08-14 and schema migration 058, as STATUS VALUES on
      decisions and conflicts. WITHDRAWN->RETIRED, PROVISIONAL->PROPOSED,
      OPEN->ACTIVE.
      SUPERSEDED was listed here until 2026-08-15 and IS NOT RETIRED. Migration
      058 dropped it on an implementer's reading that the owner had not made,
      justified by a claim that decisions.supersedes already carried the
      relation -- which was false; that column is '[]' on all 162 rows. The
      owner overturned the retirement ("yes we keep superseded") and migration
      060 restored the word, so it is a LIVE status value and must not be
      flagged. It remains in this entry's prose only as the record of a
      retirement that was reversed.
    precondition: >-
      Fails admission test 4 outright, and unlike RV-022/023/024 there is no
      spelling that separates the retired use from live correct ones. Each word
      is load-bearing elsewhere in this repo under a DIFFERENT vocabulary that
      nobody retired:
        WITHDRAWN    schemas/enums.py StandardStatus — a standards body withdrew
                     an edition. Also VerificationClosureReason.WITHDRAWN.
        PROVISIONAL  the cost_data_quality vocabulary (VERIFIED/PROVISIONAL/GREY),
                     architecture/schema-spec.md's own document status, and
                     governance/mission-PROVISIONAL.md, a filename CLAUDE.md §9-2
                     names as a redirect stub.
        OPEN         verification_disposition is literally OPEN|CLOSED (D-0157),
                     plus the SPATIAL-OPEN conflict domain and ordinary English.
      (SUPERSEDED was a fourth token here, on the grounds that the supersession
      model — governance/time-model.md §4, SupersedenceType, StandardStatus and
      every DR that supersedes another — uses the word legitimately everywhere.
      That reasoning was sound and is now moot: the status value itself was
      restored on 2026-08-15, so the word is not retired in ANY sense.)
      A literal match on any of the three would fire on hundreds of correct lines.
      Promoting one would need a match form this register does not have: scoped
      to a status assignment rather than to the word.
    note: >-
      Recorded rather than dropped because the retirement itself is real and the
      next session to grep `WITHDRAWN` will find the standards vocabulary and may
      conclude the decision word survived. The enforcement that does exist is
      better than a text match anyway: the CHECK constraints on both tables refuse
      all four, so a bad write fails at the database rather than being noticed by
      a grep. Verified 2026-08-14 against the live DB, both directions.

  - tokens: [VIS, UPL, DBL, NEU, PCS, OFS, CFS, MCAS, POTS, LCOV, SENS, EXH, IntD, ABI]
    retired_by: >-
      DR-2026-07-23-population-schema-replace (ADOPTED, DG-NON — population
      taxonomy is owner-only). Crosswalk: VIS->BLIND, UPL->LMB, DBL->DEAFBLIND,
      {NEU,PCS}->BRAIN, {OFS,CFS,MCAS,POTS,LCOV}->COM, SENS->NDV, EXH->TALL,
      IntD->ID, ABI->BRAIN.
    precondition: >-
      Fails admission test 4, and this is not close. Several tokens are CONDITION
      NAMES as well as former codes: POTS, MCAS, CFS and ABI all appear in the
      corpus as clinical terms in sentences that are correct English and correct
      evidence. `references/bpc/health-and-symptom-management/pain-ofs-built-
      environment-design.md` legitimately discusses OFS as a concept throughout.
      No regex separates the code use from the clinical use; only reading the
      sentence does. Wiring this would make the check wrong hundreds of files at a
      time, which trains readers to ignore it — the exact failure the `rejected:`
      section below exists to prevent.
      It also fails test 3 for most of its volume: the bulk sits in the research
      corpus, where a sweep is a CONTENT MIGRATION requiring per-file judgment and
      owner scope, not a text substitution.
    note: >-
      Measured 2026-08-05 over the 869 live-surface files this register scans:
      4,820 occurrences total. OFS 1,289 · VIS 840 · NEU 790 · DBL 523 · IntD 421 ·
      UPL 229 · SENS 152 · CFS 148 · MCAS 140 · PCS 93 · POTS 87 · ABI 72 · EXH 30 ·
      LCOV 6. Concentrated in references/connection-register-active.md,
      references/connections/**, references/claim-reference-join.json and the BPC
      corpus.
      Restricted to skills/ scripts/ schemas/ tools/ governance/ it is ~390
      occurrences across 267 files — a bounded, tractable sweep, and the one worth
      doing first, because those files INSTRUCT sessions. A skill that says "scope
      to UPL" makes the next session write a query against a code that does not
      exist.
      WHAT IS ALREADY ENFORCED, so this deferral is narrower than it looks: the
      DATABASE side is checked. scripts/validate_population.py (rewritten
      2026-08-05) grades every population-bearing column against the live taxonomy
      and reports retired codes with the crosswalk above. What is deferred is the
      PROSE, not the data.
      Promotion path, if the owner scopes it: add these as entries with the
      infrastructure directories in scope and the corpus deferred — which needs a
      per-entry `scope_paths` the reader does not yet support. Do not fake it with
      exempt_paths covering references/**; that would violate the licensed-mention
      rule at the top of this file and hide the larger half of the problem.

  - tokens: [DISPUTED]
    retired_by: "partially — DR-2026-08-04 §4.4"
    precondition: >-
      NOT fully retired, and this is the trap. The DR preserves DISPUTED as the
      closure reason `disputed-existence`, explicitly "because of the possibility
      that there is no resolution for something". Only its use as a
      verification_status value is retired. A bare token match cannot separate the
      two, so this fails admission test 4 as well as test 2. If promoted at all it
      must match the status use specifically, not the word.

# --- rejected: will NOT be added, and why -----------------------------------
# Adding any of these would flood the check and train readers to ignore it. Each
# was considered against the admission test on 2026-08-05 and failed.
rejected:

  - tokens: [cell_id]
    reason: >-
      Retired with RV-020/RV-021 (renamed to `specification_id` by schema 055) but
      fails admission test 4. `cell_id` is a bare column name, and
      architecture/schema-spec.md's aspirational `cell` table — a design that never
      shipped and is reconciled elsewhere — declares its own `cell_id TEXT PRIMARY
      KEY` fourteen times. No regex separates a stale reference to the renamed
      column from a correct reference to a design document's own field; only
      reading the surrounding block does. RV-020 and RV-021 already catch every
      case that matters, because a stale `cell_id` can only ever appear in a query
      that also names one of the two renamed tables. Adding it would flag the
      design spec on every run and train the reader to ignore the check — the exact
      outcome this section exists to prevent.

  - tokens: ["E-01 .. E-20 (entity codes)"]
    reason: >-
      Fails admission test 4, irreducibly. `E-08` is simultaneously the Item ENTITY
      TYPE (retired spelling) and Corridor Clear Width, a live `items.item_code`
      (correct spelling). No regex separates them; only reading the sentence does.
      DR-2026-07-21 §4 reached the same conclusion and excluded the sweep
      deliberately — it renamed the entity side precisely BECAUSE the item side is
      a data key that must not move. Re-deriving this costs an hour; do not.

  - tokens: [cross-population]
    reason: >-
      Fails admission test 3 in a way worth stating plainly: the token is retired by
      CO-0003 ("use co-occurring disability or multi-population"), but
      references/project-standards.md — the OPERATIVE ledger that carries the
      retirement — goes on to use the retired term in at least five later rules of
      its own. The rule is contradicted by its own authority. That is a finding
      requiring adjudication, not a tripwire; wiring a check to a self-contradicting
      rule makes the check wrong ~175 files at a time. Raise it as a DR.

  - tokens: ["two-marker scheme"]
    reason: >-
      Fails test 4. The drift between mission-and-epistemics.md's two-marker
      scheme and tier-system.md's operative three-marker scheme is real and is
      already recorded in CLAUDE.md §6, but it is expressed as prose about markers,
      not as a distinguishable token. The fix is to reconcile the doctrine
      documents, which is item F, not to grep for symbols that both schemes share.
