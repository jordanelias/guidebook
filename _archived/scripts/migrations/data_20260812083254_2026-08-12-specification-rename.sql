-- data_20260812083254_2026-08-12-specification-rename.sql
-- Data migration generated by scripts/emit_data_migration.py
-- Session:    2026-08-12-specification-rename
-- Generated:  2026-08-12T08:32:54+00:00
-- Summary:    fix-forward: re-encode D-0158.alternatives_considered as a JSON array to match the register model
--
-- This file is forward-only and immutable once committed. If a correction is
-- needed, emit a NEW data migration that compensates — do not edit this file.

BEGIN TRANSACTION;

-- Compensating fix-forward for data_20260812083218: D-0158's alternatives_considered
-- was written as one prose string. Every other row stores a JSON ARRAY there, and the
-- Pydantic DecisionRegister model types the field as a list, so decision_capture.py C1
-- refused to load the register at all. Re-encoded to match.

UPDATE decisions SET alternatives_considered = '["Rename in the schema phase and accept a broken rebuild \u2014 refused; a permanently red blocking gate is a disarmed gate.\n", "Withdraw or rewrite the 19 old-name data migrations \u2014 refused; they are append-only and immutable once committed.\n", "Leave the DDL in the data phase with the invariant only documented \u2014 refused; it walls the two central tables off from the numbered schema phase forever and turns every future constraint into a data migration.\n", "A compatibility view named evidence_cell_state with INSTEAD OF triggers, dropped at the end of replay \u2014 refused; its fidelity is unverifiable in the direction that matters and it returns a dead name to sqlite_master.\n", "A schema + data baseline squashing the history \u2014 deferred, not refused: it is the larger clean answer, it needs a runner change for data-migration supersession that does not exist today, and it is a separate owner decision. AFTER_DATA does not foreclose it.\n"]'
 WHERE decision_id = 'D-0158';

COMMIT;
