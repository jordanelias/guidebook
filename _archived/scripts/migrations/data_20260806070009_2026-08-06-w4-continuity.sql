-- data_20260806070009_2026-08-06-w4-continuity.sql
-- Data migration generated by scripts/emit_data_migration.py
-- Session:    session_2026-08-06-w4-continuity.md
-- Generated:  2026-08-06T07:00:09+00:00
-- Summary:    retire db_meta.schema_version — stale duplicate of PRAGMA user_version
--
-- This file is forward-only and immutable once committed. If a correction is
-- needed, emit a NEW data migration that compensates — do not edit this file.

BEGIN TRANSACTION;

-- Retire db_meta.schema_version.
--
-- Two schema-version markers existed and disagreed: this row said 11 while
-- `PRAGMA user_version` said 52. The row was seeded by scripts/init_db.py in
-- May 2026 and last touched by migration 005; every migration after that bumped
-- user_version only, so it has been forty-one versions stale and pointing at a
-- schema that no longer exists.
--
-- Nothing read it. The cost was purely legibility: CLAUDE.md §4 had to carry a
-- standing instruction to ignore it, which is the shape of a defect that has
-- been documented instead of fixed. `PRAGMA user_version` is the authority
-- (CLAUDE.md §4) and is now the only marker.
--
-- The other two db_meta rows (created_at, project) are facts that have not
-- rotted and stay.
DELETE FROM db_meta WHERE key = 'schema_version';

COMMIT;
