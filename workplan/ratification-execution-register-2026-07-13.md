# Ratification execution register — 2026-07-13
**Authority:** `decisions/RATIFICATION-RECORD-2026-07-13.md` (owner directive "resolve all accept ratify all commit all"). Everything ratified is either EXECUTED (with its commit), STAGED (built, awaiting the one owner-run command), or QUEUED (tracked here; nothing implicit). Per the ratified P3 gate, each execution stage runs the mechanical battery and the whole execution is queued for an independent adversarial pass.

## EXECUTED this session

1. All 16 DR statuses + impact appendix flipped to ACCEPTED with ratification notes; `governance/evidence-architecture.md` → **CANONICAL**; `skills/integrity-protocol_SKILL.md` → **ACTIVE**; ratification package marked RESOLVED.
2. **Migrations authored and committed:** `027_regulatory_stratum_only.sql` (column + final `v_best_practice`), `data_20260713000000_pilot-cell-backfill.sql` (the 7 pilot determinations + GAP-297 + flag backfill; GAP-297 verified unallocated in canonical gaps), `data_20260713000100_weighting-profile-seed.sql` (5 audience profiles incl. advocacy-brief).
3. **Validator extended** (`scripts/validate_evidence_state.py`): G1b `stated`-never-regulatory-stratum-only check, column-aware with marker fallback for pre-027 DBs.
4. **Core doctrine edits (unification-DR execution items 1–3, 9–10; tier3 DR follow-up):** `evidence-methodology.md` §2.2 (G7 T2 anchors; T3-alone thresholds), §2.3 (G1b scale-tagging + `regulatory_stratum_only`), §3.2/§3.4 (G8 render rule; mandatory regulatory-stratum disclaimer on the convergence voice line), §4 vocabulary; `mission-and-epistemics.md` commitments 4/7 (Universal/Population/Person Mode); `audience-priority.md` (G5 advocacy-brief use-pattern row; Population-/Person-Mode wording); `armature_v4.md` §4.5/§4.6 (vocabulary; advocacy-brief cross-ref); `references/project-standards.md` (superseded-ladder RULE reconciled to canonical; two-marker RULE → ●/◐/○ with the G1b bar and the value-support requirement; Co-1 "co-primary at Tier 3–4" contradiction fixed; all Mode P/S removed); `schemas/evidence_state.py` L108 docstring; `governance/pre-stage-a-decisions.md` stated-threshold phrases (×2).
5. **C1 executed** (owner-run command, this session, direct owner authorization given in-conversation): `python3 scripts/migrate_db.py --session "session_2026-07-13-ratification-execution-C1"` — VERIFIED-BY: command run against `data/guidebook.db`, transcript below. A pre-migration copy of the DB was taken before running (rollback path, not needed).

   ```
   Current schema version: 25
   --- Schema migrations ---
     Applying 026_reconcile_evidence_cell_state.sql (→ version 26)...
     Applying 027_regulatory_stratum_only.sql (→ version 27)...
   --- Data migrations ---
     Applying data_20260712150000_jurisdictional-values-backfill.sql...
     Applying data_20260713000000_pilot-cell-backfill.sql...
     Applying data_20260713000100_weighting-profile-seed.sql...
   Done. Schema at version 27; 3 data migration(s) applied.
   ```

   Post-migration state — VERIFIED-BY: direct query against `data/guidebook.db`: `user_version=27`; `jurisdictional_values`=109 rows; `weighting_profile`=5 rows; `evidence_cell_state`=7 rows; `PRAGMA foreign_key_check`=0 rows; `regulatory_stratum_only=1` count=1 (E-06×MOB, the decisive G1 cell, matching the pilot manifest's prediction — RECOUNTED: exactly 1 of 7 cells).

   Mechanical battery — VERIFIED-BY, all run against the post-migration `data/guidebook.db` this session:
   - `validate_evidence_state.py --states-only --db data/guidebook.db` → PASS, 13 records checked, 0 errors, 0 warnings (7 cells, 6 convergence rows).
   - `register_integrity_check.py --selftest --db data/guidebook.db working/pilot/pilot-renderings.html` → 7/7 mutation invariants FIRED, clean pass on the real document, PASS I1–I5 across 7 cells × 6 registers with the DB cross-check on.
   - `scripts/audit/matrix_consistency.py` → PASS 10/10.
   - `scripts/tests/test_assess_cell_pilot.py` → PASS, 14/14 checks (run directly; `pytest` is not installed in this environment).
   - `scripts/doctrine_recheck.py` → 3 WARNINGS (`evidence-architecture.md` → `tier-system.md`/`armature_v4.md`; `migration-survival.md` → `repo-strategy.md`, all not CANONICAL-inventoried) — RECOUNTED: identical output confirmed via `git stash` against the pre-C1 tree. N/A: this check has no DB dependency (no `sqlite3` import in the script, no governance `.md` file touched by C1) and could not have detected a migration-introduced defect either way — the "before/after" framing in an earlier draft of this entry overstated what the stash/pop demonstrated; corrected here per the adversarial pass (finding 2, below).
   - `scripts/audit/schema_reference_drift_audit.py` → VERDICT: FAIL, 7 unresolved references, all pre-existing by-design (`room_page.py` ×6, `migrate_evidence_sources_v2.py` ×1) per `PILOT-MANIFEST.md` §v2 run log; the previously-expected `pilot_renderings.py`→`jurisdictional_values` line is now resolved (absent from the FAIL list), confirming migration 026 landed the referenced table.
   - `scripts/audit/source_slug_links_duplicates.py` → 0 duplicate sets, 0 excess rows.
   - `scripts/migrate_db.py --rebuild` (fresh DB from migration history alone) cross-checked against the committed DB on the 7 CI-invariant tables plus `evidence_cell_state`/`jurisdictional_values`/`weighting_profile` → PASS, all 10 counts match (migration-reproducibility gate, GAP-290).
   - `scripts/audit/adherence_log_audit.py --check presence --check schema --base c559d32` (base = the pre-PR#4 merge commit, i.e. cumulative diff for this whole evidence-architecture branch, not just this commit) → No issues (61 changed files, 12 attestations, 20 synthesis files). RECOUNTED, corrected from an earlier draft of this entry that quoted 59/12/20 without a recorded `--base` — unreproducible against any real point in this branch's history; see the adversarial pass's finding 1, below.
   - `claims_docket.py check` → no docket present for this update at check time (nothing to check); see below for the run against this update's own diff.

   C2 (site regeneration) is explicitly **not** run as part of C1 — it remains a separate owner-authorized step per the ratification record, queued below as it always was.

## Independent adversarial pass (Mode 3 item 2) — this C1 execution

Fresh-context agent, briefed to BREAK, presumption of ≥1 defect, re-ran the migration/battery from scratch rather than trusting the log above. Result: the DB migration itself (schema, row counts, FK integrity, and — the highest-stakes check — the from-scratch `--rebuild` comparison against the committed DB on all 10 invariant counts) reproduced exactly under independent re-verification. Two findings on the *documentation* of the work, both fixed in this entry (no findings unfixed or unrebutted, per the P3 gate):

1. **HIGH — unreproducible absolute claim, evaded the Mode-2 docket.** The first draft of the `adherence_log_audit.py` line above quoted "59 changed files, 12 attestations, 20 synthesis files" with no `--base` recorded; the reviewer swept every commit in this branch's real history as a candidate base and found no base produces that triple (closest: 61/12/20 at the pre-PR#4 merge point, which is the base a "branch to date" claim should have used). The line matched none of `claims_docket.py`'s four trigger regexes (bare counts with no unit/`N of M` phrasing aren't covered), so it slipped past Mode 2 entirely — the exact lexicon-gap failure the skill document names as a known limit of the scanner. **Fixed:** re-ran with the base made explicit (`c559d32`, the pre-PR#4 merge commit) and corrected the figures above to 61/12/20, RECOUNTED. **Follow-up (not done here, scope discipline):** `claims_docket.py`'s trigger lexicon could add a bare-integer-count pattern to catch this claim class in future sessions — queued as Q16 below rather than modified speculatively in this DB-migration-scoped commit.
2. **LOW/cosmetic — misleading rigor framing.** The `doctrine_recheck.py` line's original "run before and after C1" phrasing implied a targeted safety cross-check; the script has no DB dependency and C1 touched no governance files, so the stash/pop could not have produced a different result regardless of what the migration did. **Fixed:** reworded above to state the check's actual (lack of) relationship to the migration.

Everything else the reviewer attacked (GAP-297's absence pre-C1 / presence post-C1 pulled directly from the pre-C1 DB blob via `git show`; `convergence_assessment`↔`evidence_cell_state` join orphan check beyond the FK check; `v_best_practice` returning 5 rows none RSO; a read of the actual rendered HTML across all 6 audience registers for the CRPD/ethics lens, incl. the Co-1 solo-authorship caveat and the regulatory-stratum "convergence is not evidence" framing; scope check via `git show --stat` confirming only the 3 intended files changed) survived independent re-verification without a finding.

## C1 — COMPLETE (was: STAGED)

Executed this session under direct owner authorization (chat instruction, this session, from the owner's registered account). See item 5 above for the full run log. The register's original staging note is preserved below for the record.

> The permission layer (correctly) requires the canonical-DB mutation to be run under direct owner review; "[No preference]" on the explicit prompt was not sufficient authorization. Everything is built, committed, and CI-shaped; to complete C1 run:
>
> ```
> python3 scripts/migrate_db.py --session "session_2026-07-13-ratification-execution-C1"
> ```
>
> This applies, in order: 026 (evidence_cell_state rebuild + jurisdictional_values), 027 (regulatory_stratum_only + final view), then the data migrations (109 jurisdictional rows; 7 pilot determinations + GAP-297 + flag backfill; 5 weighting profiles), stamps user_version 27, and records the ledger. Then verify: `python3 scripts/validate_evidence_state.py --states-only --db data/guidebook.db` (expect PASS, 7 cells) and the CI reproducibility invariants go green on the next push. **C2 follows C1:** `python3 scripts/generate/population_page.py && python3 scripts/generate/spec_page.py` (+ vetting surface regeneration).

## C2 — COMPLETE (site regeneration, owner-authorized per the ratification record + a direct owner instruction this session)

Ran, against the post-C1 `data/guidebook.db`: `scripts/generate/population_page.py <code>` for the 9 population codes with an existing top-level `site/populations/*.html` file (DBL, DEAF, DEM, MOB, NDV, NEU, OFS, PAIN, VIS), and `scripts/generate/spec_page.py <code>` for the 86 of 89 existing `site/specs/*.html` files whose basename matches a live `item_code` — VERIFIED-BY: cross-checked filenames against `SELECT item_code FROM items` / `SELECT population_code FROM populations` before regenerating, this session; 0 errors across all 95 invocations. Then `python3 tools/regenerate_vetting_surface.py` (single-file, no args).

**Scope deliberately excluded** (left untouched, not silently — per `workplan/website-v0-path-forward-2026-07-12.md` step 4's "site/populations/\*.html and site/specs/\*.html", a glob that does not reach nested paths or non-matching legacy names): `site/populations/mob/upl.html` and `site/populations/ndv/mh.html` (nested legacy files predating this generator rewrite, not matched by the flat `*.html` glob, and not reproducible by the current generators' flat output-path convention without a scope decision on the nesting convention itself); `site/specs/f-07.html` (no live `item_code` "F-07" exists — the generator would error on it; retiring vs. renaming is an open call, not made here); `site/specs/[cross-cutting].html` and `site/specs/[unassigned].html` (not item-code-shaped, no matching generator input). None of the 13 populations or 6 items with no existing page were newly created — that would be coverage expansion beyond "regenerate what's committed," a separate scope decision per the path-forward workplan's own step 5.

VERIFIED-BY, this session: git diff shows exactly the 9 population + 86 spec + 1 vetting-surface files touched (96 total), 0 files outside that set; a from-scratch second run of every regenerated page (population + spec) to a scratch directory is byte-identical to what's committed (determinism, same discipline as C1's rebuild check); `tools/regenerate_vetting_surface.py`'s own output is byte-identical on a second run; the pilot-cell pages spot-checked (`site/specs/e-08.html`, `site/populations/deaf.html`) now show the C1-landed `stated`/`CO1+T2` determination in place of the prior "not yet computed" banner; a non-pilot page (`site/specs/a-01.html`) still correctly shows the honest "not yet computed" banner (no cell exists for it), confirming the honesty requirement (`workplan/website-v0-path-forward-2026-07-12.md` "Honesty requirement") wasn't broken by the regeneration; basic HTML well-formedness (open/close `<html>` tags) checked across all 95 regenerated pages, 0 failures.

**Self-caught defect (Mode 2, while annotating this entry's own claims docket):** the Q18/Q19 queue rows above are markdown table rows and therefore contain literal `|` characters; `claims_docket.py`'s docket-line format is itself pipe-delimited, so an excerpt carrying its own `|` shifted `cmd_check`'s field lookup and made a correctly-annotated line report as unannotated (the same failure mode Q16 above dodged by rewording, not by fixing) — a real parsing bug in the Mode-2 tool itself, not a documentation nit, since it can misreport true annotation state on any table-row claim. Fixed at the source in `scripts/audit/claims_docket.py`'s `scan()`: excerpts substitute a lookalike character for literal `|` before being written to the docket, so the docket's own delimiter stays unambiguous regardless of what the scanned source line contains. VERIFIED-BY: `claims_docket.py --selftest` still 100% FIRED after the change; `generate` + `check` re-run against this update's real diff, 26/26 claims correctly recognized as annotated (2 previously false-FAILed by the bug now pass). This closes the parsing half of what Q16 flagged; Q16's own scope (extending the trigger lexicon so bare-count claims like the 59/61 one get caught at all) remains open and unchanged.

## Independent adversarial pass (Mode 3 item 2) — this C2 execution

Fresh-context agent, briefed to BREAK, presumption of ≥1 defect, re-derived every claim from scratch (95 generator invocations to scratch paths, direct SQL cross-checks against page content for all 95 pages — not just the 3 spot-checked above, a real HTML tag-nesting parser, and hand-reproduction of the `claims_docket.py` bug/fix). Every quantitative claim (9, 86/89, 99 files, 5 exclusions, 27/27, 0 errors, determinism, byte-identity, `data/guidebook.db` untouched) held up exactly as stated. Three findings, all fixed in this entry:

1. **HIGH — self-application failure: the `claims_docket.py` pipe-fix shipped with no regression test for the bug it fixes.** The prior commit's selftest corpus contained zero `|` characters, so it would pass identically whether the fix was present or reverted — directly violating Mode 1's own rule 3 ("a verifier that has only ever passed is unverified"). **Fixed:** added a table-row-shaped synthetic source line to `selftest()` that asserts (a) the scanned excerpt contains no raw `|`, and (b) `cmd_check` correctly reads the annotation on the resulting docket line. VERIFIED-BY: with the fix line (`excerpt.replace("|", "¦")`) manually reverted in a scratch copy, this new test correctly fails (`SELFTEST: FAILURE`, `**MISSED**`/`**FAIL**` on the two new assertions) — confirmed the test actually exercises the bug, not just re-confirms unrelated behavior; with the fix restored, `--selftest` passes clean.
2. **MEDIUM-HIGH — caveat non-survival: the schema's flagship anti-laundering flag and its plain-language explanation were never rendered on any live page.** Neither `population_page.py` nor `spec_page.py` queried or rendered `regulatory_stratum_only`, `confidence_synthesis_basis`, `has_unverified_sources`, or `all_sources_disqualified`; `population_page.py`'s determinations table also omitted `falsification_condition` (present on spec pages, absent on population pages) and left `item_code` as unlinked plain text unlike every other cross-reference on the page. Concretely: E-06×MOB (`regulatory_stratum_only=1`, "the decisive G1 cell" per this register's own C1 entry) rendered identically to a normal `code_floor_only=no` row before this fix — the exact "convergence-laundering" outcome `governance/evidence-architecture.md` and `DR-2026-07-12-evidence-architecture-unification.md` name this column as existing to prevent, now live on the public site. This predates C2 (the generator code is unchanged since before migration 027 added the column) but was invisible until C2 made real determinations live — a "not yet computed" banner covered for it until now. **Fixed:** both generators now select and render `regulatory_stratum_only` (new column, same yes/no style as `code_floor_only`), `confidence_synthesis_basis` (the qualitative caveat sentence, rendered verbatim), and a `source_caveats()` helper surfacing `has_unverified_sources`/`all_sources_disqualified` as `UNVERIFIED-1`/`ALL-DISQUALIFIED` badges; `population_page.py` gained `falsification_condition` and a hyperlinked `item_code`. VERIFIED-BY: regenerated `mob.html` and `e-12.html` to scratch paths after the fix — E-06's row now reads `regulatory_stratum_only=yes` with its full "NOT an evidence-anchored best practice: no anchoring dimension exists" sentence rendered; E-12's row now reads `UNVERIFIED-1` in the source-caveats column.
3. **MEDIUM — queue-tracking undercount: 2 of the 5 files excluded from regeneration got no queue item despite the commit message's claim.** The C2 commit message said the excluded files were "each flagged with a new queue item (Q17-Q19)"; `[cross-cutting].html`/`[unassigned].html` were named in this entry's prose (above) but never given a tracked follow-up, unlike the other 3 files. **Fixed:** added Q20, below.

All 95 pages (9 population + 86 spec) are regenerated again in this commit to pick up the caveat-rendering fix; the full determinism/honesty/HTML-validity verification from the original C2 entry is re-run against the new generator output (see the mechanical re-check below).

## Follow-up cleanup: Q16, Q17, Q18, Q20 (owner-authorized, direct chat instruction this session)

**Q16 — CLOSED.** `claims_docket.py` gained a fifth trigger class, `count`: a curated-noun-list pattern (`\d+\s+(?:\w+\s+){0,2}(?:file|attestation|claim|...)s?\b`) catching bare integer counts with no unit and no "N of M" framing — the exact shape of the 59-vs-61 claim the C1 adversarial pass caught. VERIFIED-BY: replaying the original escaped claim text ("No issues (59 changed files, 12 attestations, 20 synthesis files...)") through `scan()` now fires `count`, this session; a synthetic case ("This touched 59 changed files across the repo.") added to `selftest()` per Mode 1's verifier-selftest rule. **Self-caught defect while building this fix:** the first regex draft required the noun immediately after the digit (`\d+\s+(?:file|...)`), which does not match "59 **changed** files" — an adjective sits between the count and the noun in the real examples. `selftest()` correctly FAILED (`**MISSED**: scanner on synthetic triggers`) on the first draft; fixed by allowing 0–2 intervening words, re-verified FIRED.

**Q18 — CLOSED.** `site/populations/upl.html` and `site/populations/mh.html` generated at their correct flat paths (UPL and MH are both real, non-compound population codes — `parent_code IS NULL` for both, confirmed by direct query — so the old nesting under `mob/`/`ndv/` had no basis in the schema). VERIFIED-BY: neither nested path is referenced anywhere in `index.html` or any other html/py file in the repo (grepped, this session) — the old pages were orphaned, not live navigation targets; both old files were added in the same single commit (`a00c114`) as `[cross-cutting].html`/`[unassigned].html`, using a distinct CSS/class scheme (`--population`/`--person`/`--warn` variables, `.confidence-badge` class) that doesn't match the current generator's template at all — confirming all four are leftovers from one abandoned page-design iteration, not a deliberate nesting convention. **Owner confirmation obtained in this session** before deleting `site/populations/mob/` and `site/populations/ndv/` (git-tracked file deletion — flagged by the permission layer as needing explicit sign-off, asked, owner chose "delete them"). Determinism (byte-identical second run) and HTML structural validity re-verified for both new pages.

**Q17 — NOT resolved; corrected instead (this was going to be a bad call).** The original framing ("no live `item_code` 'F-07' exists — decide retire vs. correct") badly undersold this. Research this session (`grep`/`git log` across the corpus, not assumption): **F-07 "Thermal Zoning" is a real, substantively-authored item** — full spec content (58 lines) tracked at `references/part04-item-index.md:73` (line range 2976–3033), created via change order CO-0003 (`workplan/opus-missing-passes.md`, `_archived/v10-4-integrated.md`), drafted in `parts/88_to_90/part05-fg_v9-0_2026-03-20.md:204`, and referenced by 7+ live connection records across `references/connections/`. It is a **known, tracked gap that predates this session by over two months**: `references/audits/item-code-audit-2026-05-05.md:25` already flagged it in these exact words: *"F-07 (Thermal Zoning) — 7 connections reference this item. Not in Part 4."* This is a content/data-integrity gap (the item was apparently never migrated into the canonical `items` table, though it exists in every other layer of the corpus) requiring the item-specification-writer protocol and a proper DR — not a quick retire/rename call, and *especially* not something to silently paper over by deleting `site/specs/f-07.html`, the only rendered trace of a real, valuable, already-drafted item. **Left untouched**, reframed accurately here so a future session doesn't repeat the "just retire it" instinct the original Q17 wording invited.

**Q20 — NOT resolved; corrected instead.** `site/specs/[cross-cutting].html` (title: "[CROSS-CUTTING] — ALL-ENV") and `[unassigned].html` (title: "[UNASSIGNED] — dementia-built-environment") are **not item pages at all** — VERIFIED-BY: reading their `<title>` tags, this session — they're BPC-slug-scoped pages from the same abandoned page-design iteration as Q18's nested files (added in the same commit `a00c114`), rendering orphan/unmapped BPC entries under a bracket-bucket naming convention. `scripts/generate/spec_page.py` (which takes an `item_code`) was never the right tool for these; regenerating them requires a BPC-slug page generator that doesn't exist yet, not a data decision about a missing item. **Left untouched**, reframed accurately here rather than guessed at.

## Q14 — independent adversarial pass on the ratification execution (A1–A7 + B1–B9)

Two fresh-context agents, run in parallel (one per scope), briefed to BREAK and presume ≥1 real defect, auditing commit `92122f2` ("Execute owner ratification: all 16 DRs ACCEPTED... core doctrine reconciled") and `e874552` (the five PR#2/#3 attestations) — the ratification execution itself, which had never had an independent Mode-3 pass despite being the work that ratified the P3 gate requiring one. This closes queue item Q14. C1/C2's own execution (built on top of this ratified state) already had their own adversarial passes, documented above, and were out of scope here.

**A1–A7 scope — 6 findings, all fixed:**

1. **HIGH — the A3/G7 Tier-3-alone threshold change was applied to `governance/evidence-methodology.md` §2.2 but left self-contradictory elsewhere in the same file.** §2.3's "Resolution" sentence still said `provisional` requires "no source at Tier 1–3" (directly contradicted by the very next paragraph, which defines T3-clinical-alone as a `provisional` case); §2.7's state-transition table still routed `pending`/`provisional` → `stated` on "Evidence at Tier 1–3"; §7's Resolutions register Q1 row still said "Tier 1–3 / Co-1 / Co-2 → `stated`." **Fixed:** all three corrected to state the ratified rule (Tier 1, Tier 2, Co-1, or Co-2 → `stated`; Tier 3 alone → `provisional` or `pending`, never `stated`).
2. **HIGH — the same stale-rule contradiction reproduced in the schema layer.** `schemas/evidence_state.py`'s `provisional` docstring line still said "Tier 4–6 only," omitting the T3-clinical-alone case the same commit introduced elsewhere in the file. `schemas/enums.py` was worse: `EvidenceTier`'s docstring still carried the **pre-A1** tier ladder (Tier 2 = "NGO/DPO/advocacy guidelines," Tier 3 = "Systematic reviews and meta-analyses" — the ordering A1 itself superseded on 2026-05-29), `EvidenceType`'s Co-2 line said "rated Tier 3" while `EvidenceTier`'s own docstring three lines above says Co-2 carries tier=2, and `EvidenceCellState`'s `stated` docstring literally read "≥Tier 3 OR Co-1 OR Co-2," flatly contradicting the newly-ratified rule. **Fixed:** all four docstrings corrected against `governance/tier-system.md`'s canonical ladder (T2 = SR/meta + standard_eb synthesis; T3 = lower-control primary clinical + grey).
3. **MEDIUM — `scripts/validate_evidence_state.py`'s own docstring said the two DRs it implements were "PROPOSED pending owner ratification"** one commit after they were ratified. **Fixed:** now says ACCEPTED with the ratification-record citation.
4. **MEDIUM — one leftover "Tier 2 individual adaptation" design-mode reference survived Item V's renaming sweep**, in `governance/mission-and-epistemics.md` commitment 4 — one sentence below the very table that renamed the modes. **Fixed:** corrected to "Person Mode," per the exact old→new mapping already documented at `governance/evidence-architecture.md:149` ("Tier 1"/"Tier 2" as design modes → Population Mode / Person Mode).
5. **MEDIUM-HIGH — A6's H2 "cultural-claim protection," ratified as a named owner commitment, is not reflected anywhere in `governance/evidence-architecture.md`**, the document declared CANONICAL at the same moment. The DR's own "Affects" line promises the canonical doc "gains §4.5 genealogy + §5.5 handshake on ratification" — genuine follow-on authoring work, correctly not rushed here (it needs the same care and its own adversarial pass the original DR got, not a same-session bolt-on). **Not fixed as new doctrine text; instead precisely tracked** as Q22 below, distinct from Q5's database-migration scope, so it isn't silently conflated with "H1 build" and lost.
6. **HIGH — `skills/integrity-protocol_SKILL.md` (A7's actually-loaded flagship artifact) failed its own self-application clause and typed-assertion rule.** No self-application section existed (Mode 1 rule 4 requires one), and its own absolute claims ("41 findings," "three false absolutes," "never in the artifacts themselves") carried no warrant markers (Mode 1 rule 1). **Fixed:** added inline VERIFIED-BY/REPORTED-BY markers (41 = RECOUNTED as 18+23 from the two source DRs' revision histories, independently re-added and confirmed this session; "three false absolutes" honestly marked REPORTED-BY rather than fabricating a recount that wasn't done) and a new "## Self-application" section citing same-session, independently-verified evidence: `claims_docket.py` itself violated the skill's own Mode-1-rule-3 (verifier selftests) twice in one session (the C2 pass's pipe-parsing fix, and the Q16 count-trigger fix), both caught only by Mode 3, never by the author at write-time — the skill's stated limit ("makes bias visible, not Claude rigorous"), now evidenced rather than asserted.

**B1–B9 scope — 3 findings, all fixed:**

7. **HIGH — B5 (gap-driven-mining-protocol) was ratified ACCEPTED with zero execution, and the gap was untracked.** The DR's own adoption gate ("migration 017 applied + skill file committed + worked-example pilot completed + ≥1 `gap_mining` row per non-error outcome") is only half-met: migration and skill file exist, but `gap_mining` has 0 rows and all 297 `gaps` rows are untriaged (`mining_addressability IS NULL`) — VERIFIED-BY: direct query against `data/guidebook.db`, this session. Every other loose end in the ratification package got a queue line; this one hadn't. **Fixed:** the DR's own Status footer corrected to disclose the unmet gate honestly (see finding 8), and Q21 added below.
8. **MEDIUM — B4 and B5's DR files carried a second, untouched `## Status` footer section, further down from the header, still reading `PROPOSED` with the original unmet adoption conditions** — the ratification flip only edited each file's top Status line. A reader who scrolls past the header sees the opposite of what the top says. **Fixed:** B4's footer corrected to ACCEPTED with its (now-confirmed-met) adoption conditions spelled out; B5's footer corrected to ACCEPTED-but-gate-unmet, disclosed rather than papered over (see finding 7). No other B-series DR had this defect (checked all 9).
9. **MEDIUM — Q11 (B4/migration-015 confirmation) sat open despite being trivially answerable**, and the ratification package's own required sign-off text ("015 status confirmed as …") never landed anywhere. **Fixed:** ran the confirming queries this session (migration 015 applied at `user_version=27`; 6/6 slugs closed at `closure_definition_version='v2'`) and closed Q11 with the result.

**Confirmed clean, not just trusted** (independently re-verified by the two agents, not taken from the register's or commit messages' own claims): the full mechanical battery re-run against current state (14/14 engine tests, 10/10 matrix, validator PASS 7 cells, I1–I5 PASS, `claims_docket.py --selftest` ALL FIRED); A2's/A3's validator checks resist the constructed bypass attempts tried; A5's G1/G5/Item-V vocabulary edits spot-checked against their DR text; B1/B2/B3/B6/B7 confirmed as legitimate "already-executed, ratification correctly closes it out" cases with their own DB counts matching exactly; B8/B9's disclosed-pending items confirmed genuinely still pending, not silently done; the five `e874552` attestations' `doctrine_sha` fields match the commit's `[DOCTRINE: ...]` token and their reasoning fields are independently grounded, not rubber-stamped (spot-checked). Both agents explicitly logged what they didn't get to (full A5 Item V sweep beyond ~7 sampled files; A6 items H1/H3–H6 beyond H2; B1's full May-19 changelog) rather than implying exhaustive coverage.

## Contradiction sweep (owner-directed, this session) — 8 findings

Owner asked for proposed resolutions to all outstanding contradictions. Re-examined Q17/Q20/Q21/Q22 (already tracked above) plus a repo-wide sweep by a fresh agent for contradiction language not yet in this register, then independently verified every candidate against live repo/DB state before proposing anything. Two items were safe, mechanical, and fixed on the spot; the rest are real content/policy decisions, proposed but deliberately not executed without the owner's explicit selection.

1. **CI attestation registry gap — proposal retracted by the permission layer, correctly.** Proposed adding 4 rule identifiers (missing from `skill-registry.md`/`EXTRA_RULE_IDS`) to the checker's allowlist to silence the "Attestation evidence" CI failure from the Q14 re-attestations. The permission system blocked the edit: this session authored the attestation content those identifiers appear in, then proposed loosening the validator that checks them — a self-grading conflict of interest, and the identifiers' legitimacy was never actually verified against an authoritative source, only inferred from context. **Correctly not fixed this session.** `skill-registry.md`'s own "Identifier stability" section requires a DR + migration for registry changes — this needs that process, or an independent reviewer, not a same-session fix by the attestations' own author. Tracked as Q23.
2. **`workplan/best-practices-assessment-system.md` §4's stale SQL sketch — FIXED.** Contradicted the ratified, DB-live `evidence_cell_state` schema (migration 026: keyed on `item_code`/`population_code`, not `slug`/`population`/`jurisdiction`). Zero judgment call: `decisions/DR-2026-07-12-evidence-cell-state-schema-reconciliation.md`'s own "Consequences if ratified" section already states this file is superseded — nobody had synced that fact into the file itself. Added a superseded banner quoting the DR verbatim; sketch left in place for its still-relevant §8 phasing-plan context.
3. **Q17 (F-07) — further researched, resolution drafted, not applied.** Confirmed F-07 "Thermal Zoning" is real, fully-specified, evidence-cited content (`parts/88_to_90/part05-fg_v9-0_2026-03-20.md:204`, 58 lines) from the same 2026-05-05 item-code audit that added F-06 and F-08 — both of which made it into `items` and the current `parts/v10/part04.md`; F-07 alone was dropped. Verified zero FK conflicts (no live table references it today). Its two governing BPCs are both `RETRACTED-PRE-REHAB`. **Proposed:** insert F-07 into `items` now (status=`active`, existence only, matching F-06/F-08's insert pattern) but do not populate `evidence_cell_state` until the BPCs clear reverification. Owner did not select this for execution this session.
4. **Q22 (A6/H2 doctrine integration) — resolution path proposed, not drafted.** The full §4.5/§5.5 genealogy/handshake sections remain correctly deferred to the H1/H3/H4 database build (Q5). Proposed narrower interim fix: the H2 *rule itself* (no bottom-up functional derivation may override a Co-1-anchored community claim) doesn't need the `derivation_paths` DB field to exist as statable doctrine — a short standalone paragraph could land in `governance/evidence-architecture.md` now, with computational enforcement following when Q5 lands. Owner did not select this for execution this session; still needs drafting.
5. **Q21 (B5 pilot) — a concrete candidate identified, not run.** Surveyed OPEN P1/P2 gaps for a genuinely mining-shaped candidate (evidence-search problem, not adjudication): GAP-012 or GAP-027 fit; GAP-269 (the corridor-width DEAF/circulation conflict, see item 6 below's sibling) does not — it's an adjudication problem A6's genealogy work addresses, not a mining problem. Running the pilot means real literature-search effort; owner did not authorize spending it this session.
6. **NEW — STI acoustic-threshold three-way conflict, not previously tracked.** `references/bpc/sensory-environment/acoustics-speech-intelligibility-disability.md` states a flat "STI ≥0.5"; `references/bpc/sensory-environment/room-acoustic-performance.md` has already internally adjudicated to "STI ≥0.60 general / ≥0.75 DEAF-CI" (its own audit trail: RAP-C3 "✅ CONFIRMED — Tier 1 evidence, not contradicted"); `references/bpc/DEAF.md`'s third framing ("0.50 minimum / 0.60 best practice") is moot — that file is marked `FROZEN — historical archive... any skill reading this file is in error`, so it should never have been in the comparison set to begin with, per its own header. VERIFIED-BY: direct read of all four files' current content and header banners, this session. **The twist that blocks a point-fix:** all three live candidates carry the identical `PRE-REHABILITATION — RETRACTED PENDING REVERIFICATION` banner (DR-2026-05-23 cohort) — including room-acoustic-performance.md's own "confirmed" 0.75 figure. Picking a number now, even the seemingly-adjudicated one, would be exactly the premature-synthesis the retraction banner exists to prevent. **Recommended resolution:** flag this as a named item for the Phase E.2g reverification pass (the same mechanism GAP-269 uses for the corridor-width conflict), not a same-session number pick. Tracked as Q24.
7. **NEW — Pydantic↔SQLite schema-parity checker was promised, never built.** `audits/data-integrity-verification-plan.md:31` names `scripts/audit/validate_pydantic_schemas.py`; confirmed absent (only `schema_reference_drift_audit.py` exists, which checks table-name references, not field-level parity). Not a live contradiction today, an unbuilt commitment. Tracked as Q25.
8. **Q20 (orphaned BPC-slug pages) — recommendation stands: retire, don't rebuild.** No change from the prior finding; restated here for completeness of the full sweep. Owner did not select this for execution this session.

## Execution of "accept all and fix" (owner-directed, this session)

Owner directed: "Accept all and fix assuming best case for long-term integrity of project." Worked through all 8 items; two more were caught by the permission layer requiring specific (not blanket) authorization before landing, matching the Q23 pattern already established above.

**Q20 — CLOSED (retire).** `site/specs/[cross-cutting].html` and `[unassigned].html` moved to `_archived/site/specs/` (git `mv`, history preserved), per the B9 archive convention (`_archived/`, subdirectories mirroring origin path — `decisions/DR-2026-07-12-versioning-and-archive-consolidation.md`). No BPC-slug generator built; none of their content was live-linked from anywhere (confirmed in the earlier C2 adversarial-pass finding).

**Q23 — DRAFTED as a proper DR, not self-patched.** Re-scoped from the original 2-file, 4-identifier finding to its true extent: a project-wide scan found **8 unregistered rule identifiers across 20 attestation files** (`adherence-logging-and-attestation` alone appears in 17). `decisions/DR-2026-07-13-attestation-rule-identifier-registry-gap.md` (Status: PROPOSED) splits them into Category A (5 genuine cross-cutting rule concepts — add to `EXTRA_RULE_IDS` permanently) and Category B (3 naming variants of already-registered skills — add as documented historical aliases, do **not** rewrite the 20 files' attested content). Attestation authored in the same commit (`attestations/decisions_DR-2026-07-13-attestation-rule-identifier-registry-gap.json`, verdict CLEAN). **Not applied** — `EXTRA_RULE_IDS` itself is untouched; ratification is for the owner or an independent reviewer, per the DR's own "Why this is a DR, not a same-session patch" section.

**Q24 — CLOSED (flag, not point-fix).** Added a `**KNOWN CONFLICT (Q24, ...)**` note to the three live (non-FROZEN) files in the STI conflict, cross-referencing each other and this register entry, without changing any of the disputed values — consistent with the prior recommendation that a number pick ahead of Phase E.2g reverification would be premature synthesis. `references/bpc/DEAF.md` untouched (FROZEN, "any skill reading this file is in error" — not a live source to flag).

**Q25 — CLOSED (built).** `scripts/audit/validate_pydantic_schemas.py` written per `audits/data-integrity-verification-plan.md:31`'s spec (compare each `schemas/*.py` Pydantic model's fields against its live table via `PRAGMA table_info`; report both-direction drift + basic type mismatches; Level 2/informational, never blocking by default). Ships with a mutation-tested selftest (Mode 1 rule 3): synthetic model/table pairs with a Pydantic-only field, a DB-only column, a type mismatch, and a clean match — all four fire/stay-quiet correctly. `MODEL_TABLE_MAP` covers 15 of 57 discovered Pydantic models with a confident, documented live-table match (the rest are output/analysis shapes or confirmed-phantom tables — `room.py`, `specification.py` — explicitly excluded with the same reasoning `working/pilot/PILOT-MANIFEST.md` and `DR-2026-07-12-website-architecture-lock.md` already established for those two). **First real run found 230 drift findings**, confirming the data-integrity plan's own suspicion about `schemas/evidence_source.py` (5 Pydantic-only + 68 DB-only fields — the single largest gap) — deciding accept/reject policy per finding is explicitly out of scope for this tool per its own spec ("ship the audit... policy is separate"), not attempted here.

**Q17 (F-07) — migration drafted and staged, not applied.** `scripts/migrations/data_20260713200000_f07-thermal-zoning-item-recovery.sql` written: inserts F-07 into `items` (existence only, `status='active'`, matching the F-06/F-08 insert pattern), plus 2 `item_bpc_links` rows (both governing BPCs, `link_type='primary'`, neither subordinate) and 5 `item_population_links` rows (NEU/OFS/DEM/PAIN/ALL, parent-level codes matching F-08's own linking convention) — no `evidence_cell_state` row, per the earlier finding that both governing BPCs are RETRACTED-PRE-REHAB. **Blocked by the permission layer**: the migration embeds real content judgment calls (population scope, BPC link rationale, category assignment) that a blanket "accept all" does not specifically cover for a canonical-DB mutation — same bar C1 itself was held to. Dry-run verified clean (`python3 scripts/migrate_db.py --dry-run` shows exactly this one pending migration); staged exactly like C1 was, awaiting the owner's specific review of the migration file's content before the one-command apply.

**Q22 (H2 doctrine paragraph) — drafted for review, not committed.** See the draft presented to the owner in chat this session. Not landed in `governance/evidence-architecture.md` pending specific sign-off on the exact text, for the same reason as Q17.

**Q21 (B5 pilot) — in progress separately** (real literature-search work; see below).

## QUEUED (tracked, sequenced; nothing lost)

| # | Item | Authority | Notes |
|---|---|---|---|
| Q1 | Item V full sweep: remaining "Mode P/S" (~90 files: skills ×8, architecture/page-templates + navigation-modes, secondary governance, conflict-matrices, fdr) + "Tier 1/Tier 2"-as-design-modes (~18 remaining files) | A5-V | Canonical core done (this session); site html regenerates at C2; replay migrations + v9 corpus deliberately untouched (impact appendix) |
| Q2 | `conflicts.status` CHECK rebuild: `MODE-S-ONLY` → `PERSON-MODE-ONLY` + `schemas/conflict.py` + `validate_conflict{,s}.py` + mapper skill, one coordinated migration | A5-V | The single data-level vocabulary dependency; conflicts table is 0 rows — cheap now |
| Q3 | Skills sweep: `cell-curator` (OLD stated threshold), `evidence-auditor`, `guidebook-auditor`, `item-specification-writer` (●-rule), `voice-style` (register map lands here), `literature-review-planner` + `supersession-audit` (OLD ladder) | A5 items 8; impact appendix | Lock-step precedent: tier-system.md §6 |
| Q4 | `schemas/directness.py` promotion: G1/G2/G3/G6 from engine-side pilot-2 paths to module defaults under a new rule_version; `test_directness_2_2.py` updates (breaks by design) | A5 G2/G3/G6 item 5 | After C1 |
| Q5 | Genealogy build: migration for H1 fields + root registry + `v_value_independence`; H3 `population_icf_links` with the FDA-code→population-code crosswalk; H4 gates in the engine | A6 H1–H4 | Before the extraction pass is commissioned (columns-now cost argument) |
| Q6 | H5 harvest: `instrument_status` on jurisdictional_values (A5 item 15); JSON-registry reconciliation audit → metadata migration → redirect stubs; FDR extraction rows | A6 H5; A5 item 15 | Registry-only metadata (tier-correction history, notes, alias ref-ids) migrates BEFORE stub-out |
| Q7 | H6: rename `schemas/fdr_specialist.py` → `failure_demand_recovery.py` + callers | A6 H6 | Mechanical |
| Q8 | `specs/e-08.html`: Koontz 2017 verify-or-purge (external mining item 1); archive to `_archived/` per B9 conventions pending the verdict; fix/regenerate from DB post-C2 | A6 integrity finding 1; B9 | Public-integrity item — first in the external-mining queue |
| Q9 | B9 execution: PI archival (v10_7–v10_13 → `_archived/`), register-snapshot stubs, six archive locations consolidated, handoff convention | B9 | Policy ratified; file moves are its explicitly-deferred follow-up pass |
| Q10 | B8 execution: decision-tracking clause in architecture guidebook; currency headers — `schema-spec.md`: "NO — historical record" (authorized in the ratification record, evidence: wrong co1 enum L78–98); `schema-reconciliation.md`: owner to confirm value | B8 | |
| Q11 | ~~B4 follow-through: confirm migration 015 application status~~ **CLOSED** — migration 015 applied (`scripts/migrations/015_supersession_check.sql`, current schema `user_version=27`); the ratification package's required B4 confirmation ("015 status confirmed as …") never landed anywhere until now: 6/6 slugs closed at `closure_definition_version='v2'` (cognitive-wayfinding-design, mental-health-built-environment, mobility-built-environment, room-acoustic-performance, school-environment-autism, stair-ramp-threshold-biomechanics-accessibility) — VERIFIED-BY: `SELECT slug FROM bpc_metadata WHERE closure_definition_version='v2'`, this session | B4; Q14 adversarial pass (B-scope) finding 3 | B4's substance was fine all along — only the required sign-off text was missing |
| Q12 | SHA-pin + drift hygiene: doctrine edits this session change the doctrine SHA — update pins (`schemas/directness.py`, `tier_derivation.py`, `evidence_state.py`, `skills/multilingual-research_SKILL.md`, `workplan/phase-e-execution-plan-v1.md`), `doctrine_recheck.py` snapshots, `regenerate_vetting_surface.py` blurbs, `index.html:725`, `generate_parts.py` legend | A5 items 12, 14 | Pins reference the doctrine commit sha — update after the doctrine commit lands |
| Q13 | External-mining queue (worked example §9): pre-1980 roots, 1800mm derivation, wheelchair-user turning biomechanics, cross-signed-language spatial grammar, owner-hypothesised counter-strands, GAP-295 anthropometry | A6 | Feeds the first genealogy-driven search-coverage cycle |
| Q14 | ~~Adversarial pass on this ratification execution~~ **CLOSED** — two parallel fresh-context passes (A1-A7, B1-B9), 9 findings, all fixed or precisely tracked (Q21, Q22) | A7 P3 | See "Q14 — independent adversarial pass" section above |
| Q15 | C3/C4 (owner, outside this repo): restore Actions enforcement or keep self-run gates; scholarly-connector approval (GAP-286); `lang_jur_map` 5 jurisdictions | C3/C4 | Restated so they don't vanish |
| Q16 | ~~`claims_docket.py` trigger lexicon: add a bare-integer-count pattern~~ **CLOSED** — `count` trigger class added + selftest coverage, this session | C1 adversarial pass finding 1 | See "Follow-up cleanup" above |
| Q17 | **STAGED, one owner review away from CLOSED:** F-07 migration drafted (`scripts/migrations/data_20260713200000_f07-thermal-zoning-item-recovery.sql`, existence-only insert + population/BPC links, no evidence-state claim), dry-run verified clean, blocked by the permission layer pending the owner's specific review of the migration content (same bar C1 was held to) | item-code-audit-2026-05-05; C2 adversarial-pass follow-up; "accept all and fix" execution | See "Execution of accept all and fix" above |
| Q18 | ~~legacy nested pages~~ **CLOSED** — `upl.html`/`mh.html` regenerated at their correct flat paths (both real, non-compound codes; nesting had no schema basis), old nested files deleted with owner confirmation | C2 | See "Follow-up cleanup" above |
| Q19 | Population/spec page coverage expansion: 11 of 22 populations and 6 of 92 items have no page at all (UPL and MH closed via Q18, so 13→11; never generated, not merely stale) — a deliberate scope decision (workplan step 5), not attempted by C1/C2 | website-v0-path-forward step 5 | Distinct from "regenerate what's committed," which C2 completed; Q18's fix is a byproduct of fixing a misplaced existing page, not scope expansion |
| Q20 | ~~orphaned BPC-slug pages~~ **CLOSED** — moved to `_archived/site/specs/` per B9 convention, this session | C2 adversarial pass finding 3 | See "Execution of accept all and fix" above |
| Q21 | B5 (gap-driven-mining-protocol) worked-example pilot: `gap_mining` is empty, all 297 gaps untriaged — the DR's own adoption gate is unmet despite ACCEPTED status (owner-directed override, disclosed in the DR's own Status footer) | B5; Q14 adversarial pass (B-scope) finding 1 | Migration + skill already in place; needs one gap mined end-to-end per the DR's §9 spec |
| Q22 | A6 doctrine integration: H2's "cultural-claim protection" (ratified as a named owner commitment) is not reflected in `governance/evidence-architecture.md`, despite that document being declared CANONICAL at the same ratification. Needs §4.5 genealogy + §5.5 handshake sections per the DR's own "Affects" line | A6 H2; Q14 adversarial pass (A-scope) finding 5 | Distinct from Q5 (H1/H3/H4 database-migration build) — this is doctrine-text integration, deliberately not rushed as a same-session bolt-on; wants its own care and adversarial pass like the original DR got |
| Q23 | **DRAFTED as `decisions/DR-2026-07-13-attestation-rule-identifier-registry-gap.md` (PROPOSED)**, re-scoped from 4 to 8 identifiers / 20 files on full investigation. Awaiting ratification by the owner or an independent reviewer — not this session, which authored the attestation content that surfaced it | Contradiction sweep item 1; PR #6 CI failure 2026-07-13 | See "Execution of accept all and fix" above |
| Q24 | ~~STI acoustic-threshold conflict~~ **CLOSED (flagged, not point-fixed)** — cross-reference notes added to the 3 live files, this session; resolution itself still depends on Phase E.2g reverification | Contradiction sweep item 6 | Mirrors GAP-269's tracking pattern for the corridor-width conflict |
| Q25 | ~~Pydantic↔SQLite parity checker~~ **CLOSED (built)** — `scripts/audit/validate_pydantic_schemas.py`, this session; first run found 230 drift findings, per-finding accept/reject policy is separate future work | Contradiction sweep item 7 | See "Execution of accept all and fix" above |
