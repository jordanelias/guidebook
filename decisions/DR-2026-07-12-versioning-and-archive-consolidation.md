# DR-2026-07-12: PI archival hygiene, register-snapshot ban, archive consolidation, and handoff convention

- Status: **ACCEPTED — ratified by owner directive 2026-07-13 ("resolve all accept ratify all commit all"); see decisions/RATIFICATION-RECORD-2026-07-13.md** *(was: **PROPOSED — pending owner ratification**)*
- Date: 2026-07-12 (revised same day after adversarial review — see item 1's correction note)
- Prepared by: Claude, following `audits/consolidation-sweep-2026-07-12.md`. This DR proposes policy only; it does not itself move, merge, or delete any file. Execution is explicitly deferred to a follow-up pass once ratified (see "Consequences" below).
- **Self-review caveat:** This DR was authored in the same session that ran the sweep it responds to. `[SELF-AUTHORED — bias risk]` applies, in two directions: (a) toward finding more things worth codifying than the evidence supports, and (b) concretely, the original draft of item 1 proposed reversing an already-ratified architecture rule (new-file-per-PI-revision) while describing it as filling a gap — an independent adversarial reviewer caught this by reading `project-architecture-guidebook-v2.3.md`'s actual text before accepting the "gap" framing; this draft would not have caught it unassisted. Item 1 is corrected below; treat the correction, not the original framing, as the operative proposal.
- Affects (if ratified): `governance/project-instructions-v10_7.md`–`v10_13.md` (archival only — `v10_14.md` stays as the current file), `_archived/gap_register*.md`, `audits/_archived/tier*-verification-*.md`, `misc/archived/`, `_archived/misc-2026/`, `_archived/working-2026/`, `parts/_archived/`, `parts/deprecated/`, `parts/88_to_90/`, 10 handoff documents across `sessions/`, `references/`, `workplan/`, `working/`
- Related: `architecture/consolidation-remediation-roadmap-2026-07-12.md` (Principles 1, 2, 6), `audits/consolidation-sweep-2026-07-12.md`, `architecture/project-architecture-guidebook-v2.3.md` `<migration_and_growth>` (already states both the single-archive-location rule item 3 asks to actually apply, and the new-file-per-PI-revision mechanism item 1 leaves untouched)

## Context

Four patterns, of two different kinds: two are genuinely new policy, two are remediation of a rule the architecture doc already states. Bundled in one DR because they share a session and a root theme (git/archive history not being relied on as the version record), but each is independently ratifiable — see "What would make this ACCEPTED."

1. **[REMEDIATION] PI files accumulate instead of archiving on supersession.** `governance/project-instructions-v10_7.md` through `v10_14.md` — 8 files sit side-by-side in `governance/`. This is *not* a versioning-mechanism problem: `project-architecture-guidebook-v2.3.md`'s `<migration_and_growth>` section already prescribes drafting each PI bump as a new `governance/project-instructions-v<next>.md` file, because the PI is not API-writable and the owner needs a complete document to paste into claude.ai project settings, not a diff. That mechanism stays. The actual, narrower gap: v2.3 says how to *create* the next version but not when to *retire* the previous one — so the already-stated `<migration_and_growth>` archive rule ("move to `_archived/` rather than delete") simply isn't being applied to superseded PI files specifically.
2. **[NEW POLICY] Registers snapshotted to dated files instead of queried via git history.** The gap register exists as `_archived/gap_register.md`, `gap_register_archive.md`, and `gap_register_2026-03-20.md`; tier-verification exists as five dated files in `audits/_archived/`, one pair a single day apart. `references/connection-register.md` already demonstrates the alternative: it was replaced with a short redirect stub pointing to the two live files, rather than kept as a third parallel copy.
3. **[REMEDIATION] Six archive-ish locations exist outside the two the architecture already names** (`_archived/`, `workplan/_superseded/`): `misc/archived/`, `_archived/misc-2026/`, `_archived/working-2026/`, `parts/_archived/`, `parts/deprecated/`, `parts/88_to_90/` (89 files total). `architecture/project-architecture-guidebook-v2.3.md` already states content should move to `_archived/` mirroring its origin path — this is a compliance question, not a new rule.
4. **[NEW POLICY] Ten handoff documents with no consistent name or location** (`sessions/handoff-next-session.md`, `sessions/HANDOFF-2026-05-10.md`, `references/phase-b-handoff.md`, three `workplan/*-handoff.md` files, `references/audits/handoff-2026-05-12-*.md`, `working/mobile-app-prototype-v9/HANDOFF.md`, plus two inside a session-artifacts folder).

Note: the armature four-document chain (`armature_v3_review.md`/`v4.md`/`v4_integration.md`/`v4_resolutions.md`) is deliberately **not** included here — per the sweep's correction C, it's a design-review sequence, not version-forking, and belongs in the roadmap's P2 consolidation-into-one-doc item, not this DR's archival-hygiene or new-policy items.

## Proposed decision

1. **[REMEDIATION] PI archival hygiene.** Once a PI version is confirmed pasted-live in claude.ai (or superseded by a newer draft the owner has adopted), the previous version moves to `_archived/governance/` within the same session. The vN-per-revision mechanism itself is unchanged — this only adds the retire step v2.3's general archive rule already implies but doesn't specifically call out for PI.
2. **[NEW POLICY] Register-snapshot ban.** A register (gap register, connection register, decision register, verification-status reports) has exactly one live path. A point-in-time comparison, if genuinely needed, cites a commit SHA or embeds a git-diff excerpt rather than committing a full dated copy.
3. **[REMEDIATION] Single archive location.** All retired content consolidates under `_archived/`, subdirectories mirroring origin path, per the rule `architecture/project-architecture-guidebook-v2.3.md` already states. The six locations named above are folded into it as a follow-up execution pass (not part of this DR).
4. **[NEW POLICY] Handoff convention.** Handoff documents live at `sessions/handoffs/handoff-<topic>-<date>.md`. Existing locations get a short redirect stub (the `connection-register.md` pattern), not silent deletion.

## Consequences if ratified

- **This DR does not execute the consolidation.** Ratification authorizes a follow-up pass to: (a) move PI `v10_7`–`v10_13` to `_archived/governance/`, leaving `v10_14` as the current file at its existing path — no PI content is merged or rewritten, only relocated; (b) delete/redirect the register and verification snapshot files; (c) move the six archive-ish locations under `_archived/`; (d) relocate the 10 handoff documents. Each of these benefits from owner review before execution (e.g. confirming `v10_14` really is the version currently pasted live before archiving the rest), not a mechanical file move Claude should do unilaterally in the same pass as proposing the policy.
- Going forward, new governance-document revisions and new registers follow this policy from the start, preventing the pattern from recurring even before the backlog above is cleared.
- No code, schema, or data changes.

## What would make this ACCEPTED

The four items are independently ratifiable — the owner may accept any subset (e.g. items 2 and 4 alone, as pure new policy, without also committing to the archival execution in items 1 and 3). For items 1 and 3, review is lighter since they apply an already-ratified rule rather than propose new judgment; for items 2 and 4, review the process rule itself. An explicit ratification note or session directive naming which item(s) are accepted, followed by execution of the accepted subset as a separate pass.

## Execution record — 2026-07-21 (Phase A of `workplan/consolidation-execution-plan-2026-07-21.md`)

Executed under owner direction ("perform each in order carefully"; "Continue from where you left off"). All moves used `git mv` (history preserved) and left redirect stubs at old paths per the `connection-register.md` pattern. **A1, A2 executed; A3 resolved to documented findings; A4 deferred (see below).**

**A0 (PI-live gate).** Owner declined to re-confirm interactively and directed continuation; proceeded on the ratified default that `v10_14` is the version pasted-live in claude.ai (newest draft). If the live version is in fact older, un-archive the relevant file from `_archived/governance/` — content and history are intact.

**A1 — PI archival hygiene (item 1): DONE.** `governance/project-instructions-v10_7.md`…`v10_13.md` → `_archived/governance/`; `v10_14.md` stays live at its path. Redirect stubs left at the seven old paths. No PI content rewritten.

**A2 — single archive location (item 3): DONE.** Four archive-ish dirs folded under `_archived/`, mirroring origin path: `misc/archived/`, `parts/_archived/`, `parts/deprecated/`, `parts/88_to_90/`. (`_archived/misc-2026/` and `_archived/working-2026/` were already under `_archived/`.) A `_MOVED.md` directory-level redirect stub was left at each old path.
- **Caveat (F-07):** `_archived/parts/88_to_90/part05-fg_v9-0_2026-03-20.md` still holds the only drafted source for item **F-07 "Thermal Zoning"** — an *open, tracked* data-integrity gap (see `workplan/ratification-execution-register-2026-07-13.md`). It was archived (not deleted); its path gained the `_archived/` prefix. Any future F-07 migration must read from the new path. Flagged so it is not mistaken for dead content.

**A3 — register-snapshot ban (item 2): POLICY IN FORCE; two legacy files NOT mechanically retired — documented findings instead.** On re-verification against current files (guardrails 1 & 5), neither target is a clean redundant snapshot:
- `_archived/gap_register_2026-03-20.md` marks **92 items OPEN**, of which **30 old-scheme gap IDs are ABSENT from the live DB** (`data/guidebook.db` `gaps`, 299 rows): `GAP-B4-03..10`, `GAP-NEW-01..11`, `GAP-PROTO-01..05`, `GAP-RAP-03/06`, `GAP-S4-SYST-01/02`, `GAP-SRS-01`, `GAP-STEP5-01`, `GAP-STRUCT-01/02`, `GAP-BPC-DEM-EXT`, `GAP-DBL-BE-01`. These may be renumbered under the DB's new scheme **or** genuinely un-migrated — resolving which requires description-level reconciliation against 299 DB rows, i.e. **Phase B** work, not a mechanical Phase-A stub. Retiring the file now would risk silent loss of the only trace of possibly-unmigrated OPEN gaps. **Left in place; retirement blocked pending Phase B reconciliation.**
- The tier456 "pair a single day apart" is **not** a redundant snapshot: `tier456-verification-report-2026-04-23.md` (titled FINAL, 264 entries, 256 VERIFIED) vs `…-2026-04-24.md` (249 entries, 230 VERIFIED, **10 DUPLICATE** newly identified) — a report and its 3-minutes-later dedup revision with different totals and buckets. Neither subsumes the other; retiring either loses content. **Both left in place.**
- Minor related finding (out of A3 scope): the two DB-generated views `_archived/gap_register.md`/`_archived/gap_register_archive.md` are stale (172 IDs, generated 2026-05-08) vs the DB's 299 rows — regeneration owed (Phase B / `scripts/db.py`).

**A4 — handoff convention (item 4): DEFERRED (attestation-regime collision; needs an owner decision).** The ratified target location (`sessions/handoffs/handoff-<topic>-<date>.md`) sits inside the attestation regime's synthesis-path namespace (`^(…|sessions)/.+\.md$`). Relocating the handoffs there — or leaving `.md` redirect stubs at the four handoffs that already live under `sessions/` — puts administrative, non-synthesis files into a changeset, which `scripts/audit/adherence_log_audit.py` CHECK 0 (backfill-on-touch) then demands attestations for. Handoffs and redirect stubs are **not evidence synthesis**; writing attestations for them would be untruthful and would itself trip CHECK 8. Two ratified rules therefore genuinely conflict here. The clean fix is a small, documented CHECK 0 exemption for administrative session artifacts (the `sessions/handoffs/` convention dir + `NON-SYNTHESIS`-marked redirect stubs), consistent with the ≈255 of 268 `sessions/*.md` files that already live un-attested — **but that edits the adherence checker, which is owner-gated and must not be self-applied in the same pass that would benefit from it.** Options for the owner: (a) approve the CHECK 0 administrative-exemption; (b) relocate handoffs to a non-synthesis path (e.g. repo-root `handoffs/`), deviating from item 4's exact wording; (c) leave handoffs where they are. **A4 is held pending that decision; nothing was moved.**

**Status:** items 1 and 3 executed; item 2 policy stands with two legacy backlog files documented as findings pending Phase B; item 4 deferred pending an owner decision on the CHECK 0 collision. Phases B, C, D of the execution plan remain open.
