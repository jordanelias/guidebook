# Ratification record — 2026-07-21

**Owner directive (verbatim):** "Ratify all" — session
`claude/search-slugs-accessible-design-i9yxza` (taxonomy interrogation), 2026-07-21,
in direct response to the two PROPOSED decision records and their doctrine produced
this session, and following the owner's prior-turn adjudication of the
enterprise-legitimacy question ("something is better than nothing… 95% > 50%… so it's
fine").

## Scope ratified — this session's two decision records, in full, with every sub-item

**DR-1 — `DR-2026-07-21-held-tensions-register`** (→ ACCEPTED). Adopts as operative
doctrine:
- `governance/held-tensions.md` — the **nine** held tensions (T1–T8 pole-vs-pole; **T9
  generality vs. irreducible individuality**, the meta-tension), the four operations
  (declare / disclose / navigate / interrogate), the **anti-fig-leaf rule**, the
  unification map, and the governance bindings (no session may silently resolve a held
  tension; instruments-not-essays on content pages; the standing questions seed the
  questions-led entry surface).
- `governance/grounds.md` — the public "How This Project Thinks" render source,
  including the never-QALY commitment and the nine tensions as reader questions;
  renders at Phase E per its §R.

**DR-2 — `DR-2026-07-21-two-layer-functional-taxonomy` (v1.2)** (→ ACCEPTED). Adopts as
operative doctrine `governance/functional-taxonomy.md` v1.2:
- The **17-axis** functional register (Layer 1) and the **profile** layer (Layer 2)
  with the emergence guarantee and mapping-confidence model.
- **§0.5 the irreducibility principle** (the governing preamble) and its **owner
  adjudication** (the generalization is legitimate as the floor; the override carries
  the remainder).
- The full disposition of every population code (§4), curation rules with five
  attachment points incl. **situations** and **operational access** (§5), and the
  vocabulary reconciliation (§6).

### Fork items resolved by "Ratify all" (accepted as drafted — recorded, not silent)

Per the precedent that "ratify all" ratifies rather than defers separately-answerable
sub-items, the following open forks are resolved **as drafted in v1.2**. Each remains
reversible by a future DR; none is silently absorbed:

- **R6 — transfer capacity:** folded into `AX-WHM` (drafted default), not split.
- **R7 — cognitive granularity:** two axes `AX-COG-O` / `AX-COG-L` (drafted default),
  not the armature's four sub-dimensions.
- **R8 — respiratory / oxygen:** **decomposed** into `AX-STA` + `AX-CHM` + the egress
  thread (v1.0/drafted position), not a standalone axis. This was the fork most
  explicitly flagged "the owner adjudicates"; "Ratify all" adjudicates it to
  decomposed. Flagged here so the owner can reverse if a standalone respiratory axis
  was intended.
- **DEAF dual disposition:** ratified — axis-alias `AX-AUD` (infrastructure) **plus**
  Deaf-community identity profile holding DeafSpace / d340 (sign language as language,
  no ICD anchor).
- **EPI → `AX-SPR` situational:** ratified as drafted.
- **DBL:** ratified first-class as a compound profile (canon rule DBL ≠ VIS + DEAF
  preserved).
- **Admissions:** `OAD`, `VES`, `LCOV`, `CHD/LPA/EXH/BAR` ratified as profiles;
  **PCS split** into `PCS-TBI` + `LCOV` ratified; canonical sub-code realignment
  (OFS/ME, NDV/AUT, NEU/PCS…) ratified.
- **Queue order:** new-axis research **remains behind** the language/jurisdiction debt
  (drafted default; anti-displacement). Not reordered.

## Gates scheduled, NOT discharged by this ratification

Per DR-2 §9 and held-tensions T3/T9, ratification **schedules** these; it does not
complete them. They are pre-freeze work in the execution register:

1. **Co-1 lived-experience review gate** — pre-launch form: the two 2026-07-21
   adversarial standpoint passes stand as the form, **plus** published-Co-1 checks per
   disposition (not yet done). Full discharge requires post-launch consultation
   infrastructure, which may never exist (mission §Operational reality); the taxonomy
   remains *provisional pending lived-experience review* even while operative as the
   project's working model.
2. **Non-English concept-validation gate** — the 17 axes checked against
   `term_aliases` and the already-searched JA/DE/ZH/KO/ES/FR strata before the axis
   register is frozen. Not yet done.

## Authorized execution — staged, gated, and validated (not fired blindly)

"Ratify all" authorizes the DR-2 §9 execution, including mutation of the canonical
`data/guidebook.db`. Consistent with the migration doctrine and the P3-style
discipline in the 2026-07-13 precedent (each execution stage receives the mechanical
battery and is queued for an independent adversarial pass), execution is **staged and
tracked** in `workplan/ratification-execution-register-2026-07-21.md`, not performed
in one blind pass. The canonical-DB apply is **gated behind the two gates above** by
the ratified doctrine itself. Nothing is left implicit; execution divergences from DR
text are findings to be recorded, never silently absorbed.

The staged schema (`working/taxonomy/staged_schema_functional_axes.sql`) is **not**
promoted into `scripts/migrations/` by this record — promotion arms it for the next
CI rebuild, and must not happen until the gates pass and the apply is confirmed as a
distinct validated stage.

## Scope boundary — what "all" does and does not reach here

This record ratifies **this session's two DRs and their doctrine only**. It does **not**
unilaterally ratify adjacent PROPOSED items authored by other sessions — notably
`DR-2026-07-21-product-posture-thinking-tool-not-authority` (PROPOSED; this session's
work nests under it). If "all" is intended to include the product-posture DR (or any
other standing PROPOSED item), the owner should confirm and it will be recorded
separately rather than swept in here.

## Status changes on this ratification

| Artifact | Before | After |
|---|---|---|
| `DR-2026-07-21-held-tensions-register.md` | PROPOSED | **ACCEPTED** |
| `DR-2026-07-21-two-layer-functional-taxonomy.md` | PROPOSED | **ACCEPTED** |
| `governance/held-tensions.md` | PROPOSED | **RATIFIED / OPERATIVE** |
| `governance/grounds.md` | PROPOSED | **RATIFIED / OPERATIVE** (renders Phase E) |
| `governance/functional-taxonomy.md` v1.2 | PROPOSED | **RATIFIED / OPERATIVE** (DB exec gated) |
| `working/taxonomy/staged_schema_functional_axes.sql` | staged | staged (unchanged; promotion gated) |
| `data/guidebook.db` | — | **unchanged** (apply gated per §9) |

---

## Addendum — "Yes, ratify ALL recent work" (2026-07-21)

**Owner directive (verbatim):** "Yes, ratify ALL recent work" — extending scope, in
direct response to this record's §"Scope boundary" flag, to the remaining recent
PROPOSED decision records. Enumeration performed against `decisions/*.md` status lines
(the three genuinely-PROPOSED recent records; other grep hits were body-text matches on
already-ACCEPTED records).

### Additionally ratified — three recent PROPOSED decision records, in full

- **`DR-2026-07-13-pipeline-contract`** (→ **ACCEPTED, doctrine**). The five-stage
  spine + `pipeline-contract.yaml` as the canonical Level-2 stage-gate index.
  **Owner-action precondition outstanding (recorded, not papered over):** the stage ids
  are a repo-side reconstruction and the DR itself makes reconciliation against the
  deployed `<audit_trail>` a *ratification precondition* — an owner action I cannot
  verify. Also outstanding: the blob-vs-commit `doctrine_sha` convention inconsistency
  and the `ci.yml` doctrine-gate-vs-convention mismatch the DR flags. Doctrine accepted;
  correctness-against-deployed-reality remains owner-verifiable (register E9).
- **`DR-2026-07-20-intra-category-cross-test`** (→ **ACCEPTED**). The ICCT — every
  specification adjudicated against its category-siblings before determination (your
  2026-07-20 directive). Forks **X2** (category = functional neighbourhood, not title
  silo) and **X5** (gate strength) accepted **as drafted**, reversible by future DR.
  Execution (the `cross_test_pairs` migration + view, the `connection-discovery`
  crosstest mode, the `assess_cell.py` ICCT gate) is staged (register E10), not run.
- **`DR-2026-07-21-product-posture-thinking-tool-not-authority`** (→ **RATIFIED — already ratified on `main` (PR #46 + owner directive; commit `ac23484`); this directive reaffirms it and does not re-flip a PROPOSED record. Reconciled at the 2026-07-21 merge of `origin/main`**).
  The standing posture — adjudicates evidence, declines authority — that this session's
  entire body of work nests under; ratifying it resolves the incoherence of PROPOSED
  work nesting under a PROPOSED frame. Execution (the `mission-and-epistemics.md`
  language edit) is **staged, not applied** because it changes the doctrine blob sha
  (`0f2f525`) that every current attestation is grounded on, and must run through the
  re-attestation materiality process (register E11).

### Still out of scope (not "PROPOSED", so not ratification targets)

`DR-2026-07-13-person-mode-*` and `DR-2026-07-14-person-mode-governance-reconciliation`
are **APPLIED but execution-incomplete** (blast-radius), and
`DR-2026-07-21-re-attestation-materiality-and-window` is **RATIFIED (mechanism)** — these
are execution-completeness items, not pending ratifications. `PI-update-needed.md` is a
queued task. None is swept in by "ratify all recent work"; flagged here so the boundary
is explicit.

### Unchanged by this addendum

No canonical `data/guidebook.db` mutation; no `mission-and-epistemics.md` edit (doctrine
sha preserved at `0f2f525`); no staged SQL promoted into `scripts/migrations/`; no skill
regenerated. All execution consequences are staged in
`workplan/ratification-execution-register-2026-07-21.md`.

| Artifact | Before | After |
|---|---|---|
| `DR-2026-07-13-pipeline-contract.md` | PROPOSED | **ACCEPTED (doctrine; owner-action precondition outstanding)** |
| `DR-2026-07-20-intra-category-cross-test.md` | PROPOSED | **ACCEPTED** |
| `DR-2026-07-21-product-posture-thinking-tool-not-authority.md` | already RATIFIED on `main` | **RATIFIED (reaffirmed; not re-flipped)** |
| `governance/mission-and-epistemics.md` | `0f2f525` | **unchanged** (product-posture edit staged, register E11) |
