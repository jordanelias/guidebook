# Stage 1.5 — Governance hygiene (DONE, staged)

**Type:** staged for owner commit (decisions/ files + attestations). No PI paste required — the only PI-text consequence is queued, not bumped.

## What changed
1. **`decisions/PI-update-needed.md` corrected** (`1.5-PI-update-needed.diff`, +23/−6): the file said "Live: v10.13, Pending: v10.14 — paste needed," but **v10.14 is the operative PI this session** (header reads "V10.14 · Revised 2026-05-20"). Updated header + State section + table row to show v10.14 DEPLOYED, pending-paste closed. Documentation-only items kept explicitly queued for the next legitimate bump: the v10.16 `gap-driven-mining` line and the new DR-28-c `<hooks_status>` audit line.
2. **`DR-2026-05-28-c` authored** (`DR-2026-05-28-c-population-junctions-governance-close.md`): retrospective governance close for migration 021, which added three FK-enforced value-claim population junctions (`citation_population_links` 7, `probe_population_links` 36, `extraction_population_links` 0) mirroring the pre-existing `item_population_links` (361). FK-bound to the 22-code `populations` taxonomy; scalar `population`/`population_code` columns retained-but-deprecated (drop deferred to a caller-sweep); Level-2 `population_integrity_audit.py` enforces scalar↔junction consistency.
3. **PI `<hooks_status>` addition — queued, not bumped.** The new Level-2 audit (`scripts/audit/population_integrity_audit.py`) belongs in the PI `<hooks_status>` Level-2 list; per "no PI bump unless critical armature," it is queued in PI-update-needed.md to bundle with the next bump. CI `db_integrity` already enforces the junction FKs via `PRAGMA foreign_key_check` at the blocking level, so the PI line is documentation, not gate.

## Attestations (rule #11(b))
- `1.5-attestation-DR-2026-05-28-c.json` (new) and `1.5-attestation-PI-update-needed.json` (updates the existing one) — both schema-valid; `doctrine_sha: 3da73bd`.

## Commit (owner; decisions/ path → DOCTRINE token, timestamp last; passes check_commit_msg.py)
`governance: close DR-2026-05-28-c population junctions + correct PI-update-needed v10.14-live [DOCTRINE: 3da73bd] [2026-06-09 01:10]`
*(Assumes the Stage 1.1 doctrine commit, which mints `3da73bd`, lands first. If 1.5 is committed before 1.1, use `[DOCTRINE: 61c7f95]`.)*

## Not in 1.5 (correctly excluded)
The master-workplan phrasing "PI-update-needed's rule-vs-code drift is closed by D-A" refers to the sr_meta T2/T3 drift, which the Stage 1.1 doctrine revision (D-A) closes in the governance docs and the DB — there is no separate open rule-vs-code entry in PI-update-needed.md to edit (the rule #10 `COMPLETE-STATUTORY` drift was already closed in v10.13).
