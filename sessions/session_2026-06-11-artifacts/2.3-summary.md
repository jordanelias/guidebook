# Stage 2.3 — directness-aware cell-state machine (closes R2)

**Type:** *executed* (schema migration + model revision, staged for `main`). **Gate:** D-D. **Doctrine:** evidence-methodology §2 (cell states), §3 (convergence), §1.4/§1.6/§1.7 (scale + directness); SHA 3da73bd. Applies the Stage 2.2 model (`schemas/directness.py`).

## What it builds (R2 was: tables specified but absent)
**Migration 024** creates the two tables the doctrine makes binding:
- `convergence_assessment` — status (convergent/divergent/single_axis/pending_assessment) + clinical/co1/co2 REF-ID lists + rationale/synthesis_approach. **Directness-aware:** adds `down_weighted_sources` / `discounted_sources` recording the §1.7 grain-conditioning (anchoring set = clinical∪co1∪co2 − discounted).
- `evidence_cell_state` — one row per cell, FK to items + populations + gaps. **Directness-aware:** `design_scale` (universal/population/person) is the §1.4/§1.6 axis from which each source's directness is computed. State machine + provisional confidence-flag columns + pending gap-link + not_applicable rationale + source-quality flags.

## Cell identity decision (carefully resolved)
The doctrine's "(parameter × population)" cell is keyed on **`items.item_code`** (e.g. A-02), **not** the spec-layer SPEC-NNNN. Finding: no `specification` table exists in the migration-built DB — it is defined only in the legacy `scripts/migrate/init_database.py` (not the canonical `migrate_db.py` path), and no column anywhere holds a `spec_id`. `item_code` is the only FK-valid parameter key (and the key `spec_value_probes` already uses). `schemas/evidence_state.py` is revised to match: `spec_id`→`item_code` (regex `^[A-K]-\d{2}[a-z]?$`), plus the new `design_scale` field and the directness-conditioned convergence lists. Ripple-checked: `EvidenceStateRecord` is unused outside its own file. The cell can be refined to spec_id × population if/when that layer is built canonically.

## Verification
- **Tables:** `scripts/tests/test_evidence_cell_state_2_3.py` — **21/21 pass**: model validators (item_code incl. A-10b, design_scale, state-field rules, directness convergence) + DDL constraints (FK on item/population/gap, CHECK on state/design_scale/booleans/status, UNIQUE cell key, `foreign_key_check` clean).
- **Migration:** applied to work14.db → user_version 23→24; both tables + 3 indexes present; live FK/CHECK/UNIQUE round-trip across all four states passes.
- **Reproducibility:** full rebuild from baseline (13 schema + 160 data migrations) → user_version 24, **all 11 stable invariants match**, both tables present-and-empty in the clean replay.
- work14.db promoted to v24 cleanly (0 rows in the new tables).
`[CONFIDENCE: high — DDL constraints exercised against the shipped SQL; cell-identity grounded in DB-as-truth (no specification table) + line-reads.]`

## Commit
`schema: build evidence_cell_state + convergence_assessment directness-aware per Stage 2.3 (mig 024; cell key item_code; closes R2) [2026-06-09 02:45]`

## Carries into 2.4 / 2.5
- **2.4** wire `validate_evidence_state` to these real tables (scale/directness-aware; `pending`⇒gap-link enforcement) — the model validators here are the spec.
- **2.5** tier-consistency sweep (the 17 NULL-scope rows from 2.1).
- Populating cells (and the directness conditioning per source) is Phase E synthesis (4.3), not 2.3.
