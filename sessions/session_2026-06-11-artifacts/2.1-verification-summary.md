# Stage 2.1 — `scope` column + `(evidence_type, scope) → tier` derivation (DONE, staged, verified)

**Gate:** D-A (ruled). **Type:** *executed* — built + verified on a local rebuild, staged for `main`. **Closes R6** ("tier hand-set, not derivable").

## Artifacts (staged)
- `2.1-tier_derivation.py` → `schemas/tier_derivation.py` (new module): the `(evidence_type, scope) → tier` map as a total function over valid pairs, plus `derive_tier`, `check_tier_consistency`, `expected_scope_from_tier`.
- `2.1-022_evidence_scope_column.sql` → `scripts/migrations/022_evidence_scope_column.sql` (schema): `ALTER TABLE evidence_sources ADD COLUMN scope TEXT CHECK(scope IS NULL OR scope IN (…5 tokens…))`. Runner bumps `user_version` 21→22.
- `2.1-backfill-evidence-scope.sql` → `scripts/migrations/data_20260610172124_…sql` (data): 10 idempotent UPDATEs backfilling scope from each row's consistent tier.
- `2.1-test-tier-derivation.py`: 4 unit tests (4/4 pass).

## Design (ratified ladder, doctrine SHA 3da73bd)
`scope` disambiguates only the two evidence types that span >1 tier; the other six are tier-fixed by `evidence_type` and carry `scope='intrinsic'`:

| evidence_type | scope | → tier |
|---|---|---|
| clinical | high_control (intervention/RCT/biomechanical/sensory; **D-E** admits high-control non-OT) | 1 |
| clinical | lower_control (cross-sectional/observational/qualitative/single-centre) | 3 |
| co1 | intrinsic | 1 |
| sr_meta | intrinsic | 2 (**D-A**) |
| standard_eb | national (named-org/national EB) | 2 |
| standard_eb | international (ISO/IEC/EN) | 4 |
| co2 | intrinsic | 2 |
| grey | intrinsic | 3 |
| national_fw | intrinsic | 5 |
| code | intrinsic | 6 |

## Verification (on /tmp/work14.db with 1.3 + A.4 applied; rebuild-confirmed)
| Check | Result |
|---|---|
| `user_version` | 21 → **22** ✓ |
| scope column + CHECK | present; bogus values rejected on UPDATE ✓ |
| scope populated / NULL | **623 / 17** ✓ |
| NULL-scope set | exactly the 17 inconsistencies: clinical@T4×2, co1@T5×1, grey@T1×3, grey@T2×1, standard_eb@T3×7, standard_eb@T5×3 ✓ |
| `derive_tier` vs stored tier (623 scoped rows) | **0 mismatches** ✓ |
| `check_tier_consistency` flags | **17** (= the Stage 2.5 targets) ✓ |
| unit tests | 4/4 ✓ |
| idempotency | re-apply → 0 data migrations ✓ |
| **rebuild reproducibility** | fresh `--rebuild` (11 schema + 158 data) → user_version 22, scope 623/17; row-count invariants (evidence_sources 640, citation_mining 107, source_slug_links 689, gaps 296, connections 273, items 92) all match committed ✓ — does NOT trip GAP-290 |

`[PASS-3: clean]`

## Commit (owner; passes check_commit_msg.py — no DOCTRINE token, 2.1 doesn't modify mission)
`schema: add evidence_sources.scope + (evidence_type,scope)->tier derivation per ratified ladder (closes R6; 623 backfilled, 17 for 2.5) [2026-06-09 01:35]`

## Caveat + handoffs (honest scope)
- **scope is reverse-derived from the stored tier** in this backfill — a bootstrap that asserts only tier↔scope agreement, **not** that either matches the source's actual study design / standard level. Independent validation of scope against source content is deferred to the evidence-base passes (Phase B/E). Logged in both the module docstring and the migration header.
- **Stage 2.5** resolves the 17 inconsistencies (the NULL-scope rows); the derivation map now surfaces them mechanically (`check_tier_consistency`).
- **Stage 2.2** consolidates the scattered directness primitives onto this `scope` substrate and adds the scale-directness dimension.
