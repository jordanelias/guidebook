# Stage 4.2 — A.12 reassembly engine + parts/v10 retirement (COMPLETE)

**Workplan 4.2:** "Build the A.12 stub-mode `generate_parts.py` — the reassembly engine (closes R3-b); retire stale `parts/v10`." `[gate: Stage 2 ✓]`

Three pushed commits on `origin/main` (now `95e5466`, 19 ahead of baseline `928c438`):

| Commit | What |
|---|---|
| `29471ba` | `scripts/generate_parts.py` (engine) + `scripts/tests/test_generate_parts_4_2.py` (14 tests) |
| `9e267cb` | forward-migrate committed `data/guidebook.db` v21→v24 + reconcile the data_migrations ledger (R3-c) |
| `95e5466` | retire 21 stale `parts/v10` drafts → `parts/_archived/v10-predraft-2026-04-23/`; regenerate `parts/v10` from the committed DB |

## The engine (`generate_parts.py`)
- **Modes.** `stub` (default) renders current DB content + clearly-marked STUB blocks for everything not yet synthesised; always non-empty. `full` refuses (exit 3) until every ACTIVE BPC is COMPLETE — it currently refuses, naming the gate ("82/82 BPCs not COMPLETE; evidence_cell_state empty").
- **Idempotent.** Output is a pure function of DB state: the provenance line carries a deterministic DB *fingerprint* (hash of table counts), never wall-clock. Re-running on an unchanged DB yields byte-identical files (verified).
- **Reads** the canonical SQLite + per-slug reasoning docs; **emits** the markdown document per the file manifest (manifest + part00–part13). Real DB content flows through: 92 items (Part 4), 22 populations (Part 2), 273 connections (Part 5), the 640-source bibliography by tier/type + jurisdiction standards index (Part 13), and a live BPC rehabilitation index reflecting the RETRACTED-PRE-REHAB state (Part 3 annex). The 2.5 tier/type fixes are visible in the bibliography (T1 93 · T2 67 · T3 206 · T4 66 · T5 119 · T6 89).
- **Honours** the page-templates.md *semantics* that map: ● stated / ◐ provisional / ○ pending (C-01); no population ranking (D-NAV-006); Co-1 surfaced (D-NAV-011).
- 14/14 tests pass (non-empty 15-file set, idempotency, full-mode gate, real item codes, source counts, retraction state, schema-drift note).

## Findings surfaced (honest record)

**1. page-templates.md is the 4.4 *webpage* spec, against a schema that does not exist.** The A.12 line says "emit per architecture/page-templates.md", but that file specifies 14 URL-routed *web* templates querying `specification`, `measurement`, `jurisdictional_value`, `room`, `conflict`, `doctrine`, `economics_entry`, `case_study`, `throughline`, `specialist` — none of which are in the canonical DB (`items`, `bpc_metadata`, `connections`, `populations`, …). Template 9 sources engineering content *from* `parts/v10/part08.md`, i.e. these parts are an **input** to the webpage. So the engine emits the markdown **document** from the live schema; the webpage + its schema are Stage 4.4. `[GAP: webpage schema absent — 4.4 must either build it from the canonical DB or rewrite page-templates.md against the canonical schema.]` This is the workplan's own `[GAP: site/ render pipeline not traced]`.

**2. R3-c (ledger discrepancy) was real and is now resolved on the committed DB.** The committed `data/guidebook.db` was stale (v21, no `scope`) and its `data_migrations` ledger held **68** rows vs the **186** of the baseline the working DB was forward-migrated from. Forward-migrating it in place failed — `UNIQUE constraint failed: connections.con_id` at `data_20260516030900_restore_synthesis_scan_connections_con_0247_to_0274.sql`, whose connections already existed in the data but weren't recorded as applied. Resolution: the correct v24 state (verified identical core data on every shared table — connections 273, sources 640, items 92, authors 1244 — plus only the v22–24 table additions) was installed as the committed DB. A re-run now reports "No pending data migrations (186 already applied)" — forward-migration is clean and idempotent. `[CONFIDENCE: high — ID-set equality verified table-by-table before overwrite; nothing lost.]`

**3. R3-a (165 non-reproducible author rows) remains OPEN.** A from-scratch `--rebuild` yields 1079 `evidence_source_authors` vs the 1244 the committed DB carries — the 165 rows that exist outside the migration history. Forward-migration preserves them (the committed DB has all 1244), but the DB is still not reproducible from scratch by those 165 rows. **Stage 3.1** is the place to capture them as a migration (per ruling D-B). Not a 4.2 task; flagged.

**4. Live automation despite "CI deleted".** A scheduled/triggered job (`vetting-surface-regen: rebuild after data change`) fired on the DB push and regenerated `tools/spec-curation-vetting-surface.html`, advancing `origin/main` mid-session. The blocking gates (ci/audit) are gone, but `.github/workflows/verify-urls.yml` + `resolve-dois.yml` + a vetting-surface regen remain active and push to main. Integrated by rebase (no conflict — disjoint paths).

## parts/v10 retirement
The 21 hand-authored 2026-04-23 drafts (pre-rehabilitation, standing-rule-#10 retracted; part04 was 407 KB) were archived to `parts/_archived/v10-predraft-2026-04-23/` (+ a README explaining the retirement) rather than deleted. `parts/v10` is now 15 reproducible files (58 KB) regenerated from the committed DB — **byte-identical** whether sourced from the working DB or the committed DB. The thinness is intentional and temporary: real synthesis content renders once **Phase E (4.3)** populates the cell-state machine.

## Open / owner-pending after 4.2
- **4.3** (next): Phase E synthesis — populate `evidence_cell_state` + `convergence_assessment`; re-synthesise the 68 RETRACTED-PRE-REHAB BPCs; then `generate_parts.py` renders real content and `--mode full` becomes available.
- **3.1**: capture the 165 author rows as a migration (closes R3-a).
- **4.4**: resolve the page-templates.md schema drift (finding 1).
