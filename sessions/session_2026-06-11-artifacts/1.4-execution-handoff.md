# Stage 1.4 — Structural reconciliation: status after A.4

**Gate (1.3):** CLEARED on local `/tmp/work14.db` (ledger 68→181, schema_version→21, 0 pending). Owner still runs 1.3 once against the authoritative DB before any 1.4 migration is applied there.

## Sub-item status (workplan §4 Phase A.1–A.6; targets re-derived from sources, not the stale prose numbers)
| Sub-item | Status | Note |
|---|---|---|
| **A.4 search_languages → 1539** | **DONE — staged + verified + reproducible** | `1.4-A4-search_languages.sql`. 19 langs (skill+rule#9 agree); +5 (AR/HI/ID/SW/BN)×81=405 NOT-RUN/`[UNVERIFIED-TERMS]`. Rebuild→1539; 7 GAP-290 invariants match. See `1.4-A4-search_languages-verification.md`. |
| **A.1 slugs → 95/96** | **BLOCKED — owner modeling decision** | The 14 unmatched filesystem BPCs are population/env *aggregates* (ALL-ENV, ALL-FW, ALL-ROOMS, DBL, DEAF, DEM, IntD, MOB, NDV, NDV-MH, NEU, OFS, PAIN, VIS) + `index.md`. Workplan "missing 14" = these 14 (81+14=95 at authoring); per-topic count has since grown to 82, so the gate now implies **96**, not 95. The aggregates sit at `references/bpc/` root (no `topic_directory`, likely no search-log file); admitting them to `slugs` (FK-referenced by search tables + items.bpc_source_slug) is a model choice. **Decide: are aggregates research-slugs? all 14 or a subset? what `topic_directory`/`sl_path` do they carry?** |
| **A.5 search_coverage → 3726** | **BLOCKED — CONFLICT (24 vs 46 jurisdictions)** | `skills/multilingual-research_SKILL.md` line 52 says "all **24** jurisdictions"; DB has exactly 24; workplan A.5 + PI rule #9 say **46**. 3726 = 46×81 assumes 46. Cannot derive the jurisdiction set without resolving. Current grid is also incomplete (1864 ≠ 24×81=1944; ~80 cells missing). |
| **A.3 lang_jur_map (19×46, roles)** | **BLOCKED — same 24-vs-46 conflict + role judgment** | Schema migration (CREATE TABLE `lang_jur_map(language,jurisdiction,role)`, role∈{PRIMARY,SECONDARY,COLONIAL}; bump user_version→22) is straightforward; the 874-cell role assignment needs the resolved jurisdiction set and per-cell role judgment (only examples given in A.3). |
| **A.6 items.bpc_source_slug → 92** | **BLOCKED — content-judgment mapping** | A.6: "cross-reference items table category/name against BPC content." Each of 92 items maps to a BPC slug by *content*, not a mechanical key. Depends on A.1's final slug set. Needs an item→slug mapping pass (worth a worked sample for owner review before bulk). |

## Priority owner decision (unblocks A.3 + A.5, ~half of 1.4)
**Jurisdiction count: 24 (skill + DB) or 46 (workplan + PI rule #9)?** This is a doctrine-vs-skill conflict, not a data gap. If 46 is intended, the skill (line 52) and rule #9's "per multilingual-research_SKILL.md" are out of sync and the skill needs the 46-jurisdiction list authored first (it currently lists 24). If 24 is intended, the workplan A.5/A.3 targets (3726, 19×46) and the MASTER-workplan figures are stale. Resolve before A.5/A.3.

## What's mechanical once the two decisions land
- A.5: complete the grid to (resolved N juris)×81, new cells NOT-RUN — mechanical like A.4, after the jurisdiction list is fixed.
- A.3: CREATE TABLE migration + populate from the resolved jurisdiction set × 19 langs with roles.
- A.1: emit slug rows for the confirmed aggregate set (with agreed field values); reconcile `source_slug_links`/GAP-289.
- A.6: derive item→slug from BPC content (sample-then-bulk), FK against the post-A.1 slug set.

Verification harness for each (proven on A.4): apply to `/tmp/work14.db` → `migrate_db.py --rebuild /tmp/rebuildNN.db` → compare 7 GAP-290 invariants + the intended-change count against source → idempotency re-apply → job-owned tables untouched.
